export interface PrerequisiteProblem {
  id: string;
  problem: string;
  answer: string;
  justification: string;
}

export const prerequisiteProblems: Record<string, PrerequisiteProblem[]> = {
  "1": [
    {
      id: "prereq-prob-u1-q1",
      problem: "A company has three departments: Engineering (50 hosts), HR (20 hosts), and Finance (10 hosts). The network工程师 is assigned the 192.168.1.0/24 block. Design a subnetting scheme that gives each department its own subnet while conserving addresses. List each subnet's network address, broadcast address, usable host range, and subnet mask.",
      answer: "Engineering: 192.168.1.0/26 (62 usable hosts, range .1–.62, broadcast .63), HR: 192.168.1.64/27 (30 usable hosts, range .65–.94, broadcast .95), Finance: 192.168.1.96/28 (14 usable hosts, range .97–.110, broadcast .111). Remaining .112–.255 reserved for future growth.",
      justification: "Engineering needs 50 hosts → /26 provides 2^6 − 2 = 62 usable addresses. HR needs 20 hosts → /27 provides 30 usable. Finance needs 10 hosts → /28 provides 14 usable. VLSM (Variable Length Subnet Mask) allocates the largest subnets first, preventing address overlap and leaving a contiguous reserved block for future use.",
    },
    {
      id: "prereq-prob-u1-q2",
      problem: "A user reports they can access http://www.example.com but NOT https://www.example.com. Both resolve to the same IP address. The network uses a stateful firewall. List the possible causes and the diagnostic steps you would take to isolate the issue.",
      answer: "Possible causes: (1) Firewall blocks outbound port 443 (HTTPS) but allows port 80 (HTTP). (2) The web server is not listening on port 443. (3) An intermediate proxy or load balancer drops TLS handshakes. (4) MTU issues cause TLS packet fragmentation. Diagnostic steps: (a) Use 'telnet <ip> 443' or 'nc -zv <ip> 443' to check if port 443 is reachable. (b) Run 'curl -v https://www.example.com' to observe where the TLS handshake fails. (c) Check firewall logs for denied packets on port 443. (d) Use 'openssl s_client -connect www.example.com:443' to inspect the TLS certificate and handshake.",
      justification: "HTTP uses port 80 (plaintext) and HTTPS uses port 443 (TLS-encrypted). Since HTTP works, Layer 3/4 connectivity to the server is intact. The failure is port-specific or TLS-specific. Systematic elimination using port scanning (step a), protocol-level diagnostics (step b, d), and firewall log inspection (step c) isolates whether the block is at the firewall, server, or intermediary device.",
    },
    {
      id: "prereq-prob-u1-q3",
      problem: "You are given the network topology below: Router A connects to Router B via a /30 link (10.0.0.0/30). Router B connects to a switch, which connects to three servers (172.16.1.0/24). Router A also connects to the Internet. A host on 172.16.1.10 cannot reach the Internet. Describe the troubleshooting steps you would follow, referencing the OSI model.",
      answer: "Step 1 (Layer 1 — Physical): Verify the server's NIC link light is on, switch port LED is active, and all cables are seated. Step 2 (Layer 2 — Data Link): Check the switch's MAC address table to confirm the server's MAC is learned on the correct port. Verify no VLAN mismatch. Step 3 (Layer 3 — Network): On the server, check 'ip route' for a default gateway. Ping the gateway (172.16.1.1). Ping Router B's LAN interface. Ping Router B's WAN interface (10.0.0.1). Ping Router A (10.0.0.2). Step 4 (Layer 4 — Transport): If ping works but HTTP fails, test 'telnet 8.8.8.8 80' to check if specific ports are blocked. Step 5 (Check NAT/ACL on Router A): Verify NAT translation rules and outbound ACLs permit traffic from 172.16.1.0/24.",
      justification: "The OSI layered troubleshooting model ensures you rule out lower-layer issues before investigating higher layers. A Layer 1 issue (cable) would cause complete failure. A Layer 2 issue (VLAN mismatch) would prevent ARP resolution. A Layer 3 issue (missing default route) would prevent Internet reachability. Higher-layer issues (NAT, ACLs, DNS) only matter once connectivity is established.",
    },
    {
      id: "prereq-prob-u1-q4",
      problem: "A web application uses TCP port 8443. A client behind a corporate firewall can access external websites on port 443 but not the application on port 8443. The server is confirmed to be listening. The firewall allows outbound HTTP (80) and HTTPS (443) only. Propose two solutions and explain which is better from a security standpoint.",
      answer: "Solution 1: Add a firewall rule to allow outbound TCP port 8443 (explicit allow). Solution 2: Place a reverse proxy/load balancer on port 443 in the DMZ that forwards traffic to the application server on port 8443 internally. Solution 2 is better from a security standpoint because: (a) It does not require opening additional outbound ports on the corporate firewall. (b) The reverse proxy can perform TLS termination, URL filtering, rate limiting, and WAF (Web Application Firewall) inspection. (c) The internal server port 8443 is never exposed to the Internet directly. (d) It centralizes security policy at the proxy rather than distributing rules across firewalls.",
      justification: "Opening port 8443 on the corporate firewall increases the attack surface — any host could use that port. A reverse proxy acts as a security gateway: it inspects traffic, enforces authentication, and shields the backend server. This follows the defense-in-depth principle and the principle of least privilege.",
    },
  ],

  "2": [
    {
      id: "prereq-prob-u2-q1",
      problem: "An NMS is polling 500 devices every 60 seconds using SNMP GET for interface counters (ifInOctets, ifOutOctets, ifSpeed on 4 interfaces per device). Calculate the approximate SNMP traffic volume per polling cycle. If the NMS CPU is at 95%, suggest three strategies to reduce load.",
      answer: "Per device: 4 interfaces × 3 OIDs = 12 SNMP GET requests. Each request: ~100 bytes (UDP header + SNMP header + OID). 500 devices × 12 = 6,000 GET requests × 100 bytes ≈ 600 KB per cycle, plus ~6,000 responses (~800 KB). Total ≈ 1.4 MB per 60-second cycle. Strategies: (1) Increase polling interval to 120–300 seconds (reduces frequency). (2) Use SNMP GETBULK to retrieve multiple OIDs in a single request (reduces request count by ~12x). (3) Use SNMP Inform/Streaming Telemetry (push model) instead of polling — devices push data at configured intervals without NMS-initiated requests.",
      justification: "SNMP GET retrieves one OID per request; GETBULK (SNMPv2c/v3) retrieves an entire subtree in one request, dramatically reducing request count and per-request overhead. Increasing intervals trades granularity for CPU. Push-based telemetry eliminates polling entirely, reducing NMS CPU to near zero for data collection.",
    },
    {
      id: "prereq-prob-u2-q2",
      problem: "A network engineer writes a NETCONF/YANG configuration for a router interface but the device rejects it with: '<rpc-error><error-tag>invalid-value</error-tag>'. The configuration is: <interface><name>ge-0/0/0</name><ipv4><address><ip>10.0.0.1</ip><prefix-length>33</prefix-length></address></ipv4></interface>. Identify the error and explain how YANG's data model prevents this.",
      answer: "The error is that prefix-length 33 is invalid for IPv4 — valid values are 0–32 (an IPv4 address has 32 bits). The YANG model for the interface defines prefix-length with a type that includes a 'range' constraint (e.g., 'type uint8 { range '0..32'; }'). NETCONF's validation layer checks the configuration against the YANG schema before applying it, catching this error at commit time. This prevents invalid configurations from reaching the device's running config, avoiding runtime failures or security issues.",
      justification: "YANG's strength over CLI or SNMP SET is that it defines strict data types, ranges, enumerations, and constraints. The NETCONF server validates every configuration change against the YANG model. This is 'schema-driven validation' — it catches errors at the management layer, not after the config is applied. CLI-based configuration has no such guardrails.",
    },
    {
      id: "prereq-prob-u2-q3",
      problem: "You are integrating a new network device that supports both SNMP (v2c) and NETCONF. The NMS needs to (a) monitor interface status in real time, (b) retrieve the full running configuration, and (c) push a configuration change. For each task, recommend whether to use SNMP or NETCONF and justify your choice.",
      answer: "(a) Monitor interface status: SNMP TRAP/INFORM for event-driven notifications (link up/down) + SNMP GETBULK for periodic polling of ifTable. SNMP is lightweight, well-supported for monitoring, and its MIB-II ifTable is universally implemented. (b) Retrieve full running configuration: Use NETCONF <get-config>. SNMP can retrieve specific OIDs but cannot return a complete, structured device configuration. NETCONF returns the full config in a single RPC as structured XML. (c) Push configuration change: Use NETCONF <edit-config> with YANG model validation. SNMP SET can only write individual scalar MIB values — it cannot perform transactional, multi-change configuration with rollback. NETCONF provides atomic commit, rollback-on-error, and config validation.",
      justification: "SNMP excels at lightweight, read-heavy monitoring with broad device support. NETCONF excels at configuration management with structured data models, transactional semantics, and full-device config retrieval. Using the right protocol for each task avoids limitations (SNMP can't do config management well; NETCONF's overhead is unnecessary for simple monitoring).",
    },
    {
      id: "prereq-prob-u2-q4",
      problem: "A REST API for a network controller returns the following JSON response for a device inventory query. Identify at least 3 data quality issues and explain how proper API design would prevent them.\n\n```json\n{\"devices\": [{\"ip\": \"10.0.0.1\", \"hostname\": \"\", \"status\": \"active\", \"model\": \"N/A\", \"interfaces\": null}]}\n```",
      answer: "Issues: (1) Empty hostname ('') — should either be null/absent or return a meaningful default; empty strings are ambiguous. (2) 'N/A' as a string for model — should use null to indicate unknown, not a human-readable string that pollutes data. (3) interfaces: null — should be an empty array [] to maintain consistent type (always an array), preventing null-check errors in clients. Additional issues: No device ID field (IP is not a stable identifier); no 'last_seen' timestamp for staleness detection; no HATEOAS links for API discoverability. Proper API design: Use OpenAPI/Swagger schema with required fields and nullable types. Use consistent null semantics (null = unknown, absent = not applicable). Validate responses against schema before returning.",
      justification: "API contracts should be machine-readable and strictly typed. Empty strings vs null vs absent have different semantic meanings. Clients should not need defensive coding for every field. Schema validation (OpenAPI) at design time and response validation at runtime prevent these issues from reaching consumers.",
    },
    {
      id: "prereq-prob-u2-q5",
      problem: "An NMS needs to walk the entire MIB-II interface table (ifTable) for a device at IP 10.0.0.5 using SNMPv2c community 'public'. Write the sequence of SNMP operations that would be performed and explain why GETNEXT is used instead of repeated GET requests.",
      answer: "The NMS performs an SNMP WALK starting at OID 1.3.6.1.2.1.2.2 (ifTable): Step 1: GETNEXT 1.3.6.1.2.1.2.2 → returns OID 1.3.6.1.2.1.2.2.1.1.1 (ifNumber for interface 1). Step 2: GETNEXT 1.3.6.1.2.1.2.2.1.1.1 → returns 1.3.6.1.2.1.2.2.1.1.2 (ifNumber for interface 2). Step 3: GETNEXT 1.3.6.1.2.1.2.2.1.2.1 (ifDescr) → returns interface 1 description. This continues until GETNEXT returns an OID outside 1.3.6.1.2.1.2.2 (e.g., moves to ifType under a different subtree), signaling end of table. Why GETNEXT: You cannot use GET because you don't know all the OIDs in advance — ifTable rows are indexed dynamically (interfaces can be added/removed). GETNEXT automatically discovers the next valid OID, handling variable table sizes without pre-configuration.",
      justification: "MIB tables use INDEX clauses that create dynamic OID suffixes (e.g., ifIndex can be 1, 2, 5, 100 — not necessarily sequential). GETNEXT traverses the OID tree lexicographically, returning whatever the next valid OID is. This makes it table-walk-safe regardless of how the device indexes its interfaces. GETBULK (SNMPv2c+) is even more efficient, returning multiple rows per request.",
    },
  ],

  "3": [
    {
      id: "prereq-prob-u3-q1",
      problem: "A core router fails at 10:00 AM. By 10:02 AM, the NMS has received 247 TRAP messages from various devices: 180 'linkDown' TRAPs from access switches, 40 'ospfNeighborLoss' from distribution routers, 25 'bgpPeerBackwardTransition' from edge routers, and 2 'systemRestart' from the core router itself. Describe the alarm correlation process and identify the root cause alarm.",
      answer: "Correlation process: Step 1 — Temporal correlation: All 247 alarms occurred within a 2-minute window (10:00–10:02), suggesting a single cascading event. Step 2 — Causal analysis: The 2 'systemRestart' TRAPs from the core router at 10:00 are the earliest alarms. The 40 OSPF neighbor loss alarms from distribution routers are a direct consequence (they lost adjacency with the failed core router). The 180 linkDown alarms from access switches are a secondary effect (their uplinks through distribution routers went down). The 25 BGP alarms are edge routers losing routes via the core. Step 3 — Root cause identification: The 'systemRestart' from the core router is the root cause alarm. All 245 other alarms are derived/dependent alarms. Step 4 — Suppression: The NMS should suppress the 245 derived alarms and surface only the core router restart as the actionable alarm.",
      justification: "Alarm correlation uses topology awareness, temporal proximity, and causal dependency to reduce alarm floods. A single device failure can generate hundreds of alarms within seconds (the 'alarm storm' problem). Without correlation, operators are overwhelmed. Root cause analysis identifies the originating fault; alarm suppression reduces noise by hiding derived alarms.",
    },
    {
      id: "prereq-prob-u3-q2",
      problem: "An NMS uses SNMP polling with a 5-minute interval. A critical link fails at 10:01:30 and is restored at 10:03:15 (1 minute 45 seconds of downtime). The NMS polls at 10:00, 10:05, and 10:10. Will the NMS detect this failure? If not, explain why and propose a solution.",
      answer: "The NMS will NOT detect this failure through polling. The poll at 10:00 shows link UP. The link fails at 10:01:30 and is restored at 10:03:15 — before the next poll at 10:05. The poll at 10:05 sees the link UP again. The failure window (10:01:30–10:03:15) falls entirely between two polls and is invisible to the polling-based NMS. This is the 'polling gap' problem. Solution: Enable SNMP TRAP generation on the device for link state changes (ifOperStatus traps). The device sends an immediate 'linkDown' TRAP at 10:01:30 and a 'linkUp' TRAP at 10:03:15, giving the NMS real-time visibility regardless of poll timing. For critical infrastructure, combine polling (for periodic state verification) with TRAPs (for immediate event notification).",
      justification: "Polling-based monitoring has an inherent detection delay equal to the polling interval. Short-duration failures that start and end between polls are invisible. Event-driven notifications (TRAPs, INFORMs, streaming telemetry) provide immediate visibility for state changes. The industry standard is to use both: TRAPs for real-time alerting and polling for periodic state reconciliation and trap-loss recovery.",
    },
    {
      id: "prereq-prob-u3-q3",
      problem: "You are designing an incident management workflow. A network alarm is generated at 10:00 AM. The SLA requires critical alarms to be acknowledged within 15 minutes and resolved within 2 hours. Design the workflow steps, escalation rules, and how the ticket system tracks compliance.",
      answer: "Workflow: 10:00 — Alarm generated → auto-created as Incident Ticket (Priority: Critical, Severity: P1). 10:00 — Auto-assigned to NOC L1 operator based on alarm type and on-call schedule. 10:05 — L1 acknowledges ticket (SLA clock pauses during acknowledgment check). 10:05–10:20 — L1 performs initial triage: checks alarm correlation, identifies probable root cause, checks known issues. 10:20 — If L1 cannot resolve → escalates to L2 (network engineer) with diagnosis notes. 10:35 — L2 acknowledges escalation. 10:35–11:30 — L2 performs deep diagnostics, implements fix. 11:30 — L2 marks resolved, triggers verification tests. 11:35 — L1 verifies service restoration, closes ticket. Escalation rules: If L1 does not acknowledge within 15 min → auto-escalate to L2 + SMS alert. If L2 does not acknowledge within 30 min → escalate to L3 (network architect) + page. If resolution exceeds 90 min → notify NOC manager. SLA tracking: Dashboard shows time-to-acknowledge, time-to-resolve, SLA compliance rate (%), aging tickets, and escalation frequency.",
      justification: "Structured incident workflows ensure consistent response, accountability, and SLA compliance. Escalation rules prevent single points of failure in the response chain. Time tracking at each stage enables performance measurement, capacity planning, and continuous improvement. Without defined workflows, incidents fall through cracks and SLA breaches go unnoticed.",
    },
    {
      id: "prereq-prob-u3-q4",
      problem: "An NMS needs to build a network topology map automatically. It discovers three routers: R1 (10.0.0.1), R2 (10.0.0.2), R3 (10.0.0.3). Using LLDP and SNMP, describe how the NMS determines which devices are connected to which, and how it discovers devices beyond these three.",
      answer: "Step 1 — SNMP sysDescr: Query each device's sysDescr (OID 1.3.6.1.2.1.1.1) to identify device type, OS, and capabilities. Step 2 — LLDP neighbor discovery: Query lldpRemTable (OID 1.0.8802.1.1.2.1.4.1) on each device. R1 reports LLDP neighbor 'R2' on port ge-0/0/1. R2 reports neighbors 'R1' on ge-0/0/2 and 'R3' on ge-0/0/1. R3 reports neighbor 'R2' on ge-0/0/2. Step 3 — Build adjacency graph: R1 ↔ R2 ↔ R3. Step 4 — Discover beyond: Query each router's IP routing table (ipRouteTable, OID 1.3.6.1.2.1.4.21) to find next-hop IPs. Query ARP tables (ipNetToMediaTable) for MAC addresses. Use LLDP-MED or CDP to discover access-layer switches connected to each router. Step 5 — Recursive discovery: For each newly discovered device IP, repeat SNMP + LLDP queries to discover their neighbors, building the topology iteratively until no new devices are found.",
      justification: "LLDP (Link Layer Discovery Protocol) provides neighbor identity at Layer 2, while SNMP provides device identity and routing information at Layer 3. Combined, they enable automated topology construction without manual configuration. Recursive discovery from known devices outward is how commercial NMS platforms (e.g., Cisco Prime, SolarWinds) build complete network maps.",
    },
    {
      id: "prereq-prob-u3-q5",
      problem: "A network team receives a REST API response from a controller: HTTP 200 with body {\"status\": \"partial\", \"results\": [{\"device\": \"R1\", \"code\": 200}, {\"device\": \"R2\", \"code\": 500}, {\"device\": \"R3\", \"code\": 200}]}. The team applied a configuration change to three devices. Explain what happened, how this differs from a transactional NETCONF commit, and how to handle this in an automated workflow.",
      answer: "What happened: The REST API applied the configuration change to R1 and R3 successfully (HTTP 200), but R2 returned an error (HTTP 500 — server-side failure, possibly due to invalid config, connectivity issue, or device overload). The overall operation is 'partial' — some devices succeeded, others failed. This differs from NETCONF: NETCONF's <commit> operation is atomic — either ALL devices apply the change or NONE do (with rollback on failure). The REST API has no built-in rollback, leaving R1 and R3 with the new config while R2 has the old config — an inconsistent state. Automated workflow handling: (1) Parse the partial response. (2) For failed devices (R2), retry with exponential backoff. (3) If retry fails, trigger rollback on R1 and R3 (if the API supports it) to restore consistency. (4) Log the inconsistency for manual investigation. (5) Consider migrating to NETCONF for configuration operations requiring atomicity.",
      justification: "REST APIs typically do not provide cross-device transactional semantics. Each HTTP request is independent. This creates consistency risks when managing multiple devices atomically. NETCONF's commit/rollback model is designed for this use case. When using REST for configuration, application-layer transaction management (saga pattern, compensating transactions) is required to maintain consistency.",
    },
  ],

  "4": [
    {
      id: "prereq-prob-u4-q1",
      problem: "A telecom operator wants to deploy a virtual firewall (VNF) to replace a physical appliance. The physical firewall handles 10 Gbps throughput. The VNF is deployed on a single VM with 4 vCPUs and 8 GB RAM. Performance testing shows only 2 Gbps throughput. Analyze three possible root causes and propose solutions for each.",
      answer: "Root cause 1 — Insufficient vCPU count: Firewall inspection (stateful packet filtering, DPI) is CPU-intensive. 4 vCPUs may be saturated. Solution: Scale horizontally (deploy multiple VNF instances behind a load balancer) or scale vertically (increase to 8+ vCPUs). Root cause 2 — Single-threaded packet processing: If the VNF uses a single-threaded packet processing model, only 1 vCPU is utilized. Solution: Enable multi-queue NIC (RSS — Receive Side Scaling) and ensure the VNF uses DPDK (Data Plane Development Kit) for multi-core packet processing. Root cause 3 — Memory/CPU pinning not configured: Virtual CPU scheduling by the hypervisor introduces latency. NUMA (Non-Uniform Memory Access) misalignment causes memory access delays. Solution: Configure CPU pinning (vCPU pinned to physical core), huge pages (reduce TLB misses), and NUMA-aware memory allocation in the VIM (OpenStack: flavor with cpu-pinning, huge-pages).",
      justification: "VNF performance depends on both the VNF software and the virtualized infrastructure. Physical appliances have optimized hardware (ASICs, NPUs) that VMs lack. Achieving equivalent performance requires DPDK for userspace packet processing, SR-IOV for hardware-level NIC bypass, and proper resource allocation. This is why NFV performance engineering is a critical skill.",
    },
    {
      id: "prereq-prob-u4-q2",
      problem: "An operator uses Ansible to configure 100 switches with VLAN 100 (name: 'PRODUCTION'). Switches 1–95 accept the config, but switches 96–100 fail with: 'Invalid VLAN name: must be 1-32 characters'. Investigate the issue and design a robust automation approach.",
      answer: "Investigation: Switches 96–100 are likely running an older OS version that enforces stricter VLAN naming rules, or the Ansible playbook contains a hidden character (non-printable Unicode) in the VLAN name for those devices. Verification: (1) SSH into switch 96 and manually run 'vlan 100; name PRODUCTION' to check if it works. (2) Inspect the playbook YAML bytes for hidden characters. (3) Check if switches 96–100 use a different vendor/model with different syntax. Robust automation approach: (1) Pre-flight checks: Ansible playbook first queries each device's OS version and validates compatibility. (2) Input validation: Use Ansible's 'assert' module to validate variables before applying. (3) Idempotent design: Use 'replace' or 'config' module with 'backup: yes' to save current config before changes. (4) Rollback: If any device fails, automatically restore from backup. (5) Dry-run: Use '--check' mode to preview changes without applying. (6) Test in staging: Validate playbook against a lab switch matching the production OS version.",
      justification: "Automation at scale amplifies small errors. A typo that affects one device becomes a outage affecting hundreds. Robust automation includes pre-flight validation, idempotency, rollback capability, and staged rollouts. Ansible's 'check mode' and 'diff mode' enable safe testing. Backup-before-change ensures recovery is always possible.",
    },
    {
      id: "prereq-prob-u4-q3",
      problem: "A DevOps team wants to monitor a microservices-based network management application. They need to track: (a) API request latency, (b) error rates, (c) database query performance, and (d) container resource usage. For each metric, recommend the appropriate telemetry type (metrics, logs, traces) and a tool/format.",
      answer: "(a) API request latency: Distributed tracing (OpenTelemetry/Jaeger) — trace each request from ingress through microservices, capturing span durations. Also emit RED metrics (Rate, Errors, Duration) as Prometheus counters/histograms. (b) Error rates: Structured logs (JSON) with severity levels (ERROR, WARN) shipped to ELK (Elasticsearch + Logstash + Kibana) or Loki. Also emit as Prometheus counter with 'status' label for alerting. (c) Database query performance: Application-level metrics (query duration histogram, connection pool usage) exported to Prometheus. Slow query logs to ELK for root cause analysis. Distributed traces show which queries cause latency. (d) Container resource usage: cAdvisor (Container Advisor) exports CPU, memory, network, disk metrics per container to Prometheus. Kubernetes metrics-server provides real-time resource data for HPA (Horizontal Pod Autoscaler). Combined: Prometheus for metrics (time-series, alerting), OpenTelemetry for traces (request flow), ELK/Loki for logs (detailed debugging), Grafana for unified dashboards.",
      justification: "The three pillars of observability — metrics, logs, and traces — each serve different purposes. Metrics (aggregated numerical data) answer 'what is happening now?'. Logs (discrete event records) answer 'why did it happen?'. Traces (request flow through services) answer 'where is the bottleneck?'. No single telemetry type is sufficient; production systems need all three.",
    },
    {
      id: "prereq-prob-u4-q4",
      problem: "A network team wants to implement CI/CD for network device configurations. Currently, engineers manually SSH into devices and apply changes via CLI. Design a CI/CD pipeline for network configuration that includes version control, testing, and deployment stages.",
      answer: "Pipeline design: Stage 1 — Version Control: All device configurations stored in Git repository (e.g., 'network-configs/'). Each change is a pull request (PR) with description and reviewer. Stage 2 — Linting & Validation: Automated pre-commit hooks validate syntax (e.g., yanglint for YANG models, ansible-lint for playbooks). CI runner (GitHub Actions/GitLab CI) checks for compliance with organizational standards (naming conventions, IP schemas). Stage 3 — Lab Testing: Spin up virtual network topology (containerlab, EVE-NG, GNS3) using the same device images. Apply the configuration change to virtual devices. Run automated tests: ping reachability, BGP adjacency, VLAN connectivity. Stage 4 — Staged Deployment: Deploy to production in phases: (a) single non-critical access switch, (b) all access switches in one site, (c) distribution layer, (d) core/edge. Wait for monitoring signals (no new alarms) between stages. Stage 5 — Rollback: If monitoring detects anomalies, automatically revert to previous Git commit and re-apply. Stage 6 — Audit: All deployments logged with commit hash, author, timestamp, and diff.",
      justification: "CI/CD for networks applies the same principles as software CI/CD: version control (auditability, history), automated testing (catch errors before production), staged rollout (blast radius control), and rollback (quick recovery). This replaces 'SSH and pray' with disciplined, repeatable, auditable change management.",
    },
    {
      id: "prereq-prob-u4-q5",
      problem: "An operator is migrating from a physical ADC (Application Delivery Controller) to a virtual ADC deployed as a VNF on OpenStack. The physical ADC handles 5 Gbps with SSL offloading. The virtual ADC VM has 8 vCPUs, 16 GB RAM, and a 10 Gbps virtio NIC. During testing, SSL offloading throughput is only 500 Mbps. Design an optimization plan to reach 5 Gbps.",
      answer: "Optimization plan: Phase 1 — CPU optimization (target: 2 Gbps): (a) Enable DPDK for SSL processing in userspace (bypass kernel overhead). (b) Configure CPU pinning to dedicated physical cores (avoid hypervisor context switches). (c) Enable AES-NI hardware acceleration (verify host CPU supports it via 'grep aes' /proc/cpuinfo). Phase 2 — NIC optimization (target: 3.5 Gbps): (a) Replace virtio with SR-IOV virtual function (VF) for direct hardware NIC access, eliminating hypervisor switching. (b) Enable multi-queue (16 queues) with RSS for parallel packet processing across vCPUs. (c) Increase ring buffer size to reduce packet drops under load. Phase 3 — SSL optimization (target: 5 Gbps): (a) Use session resumption (TLS tickets) to reduce per-connection SSL handshake overhead. (b) Enable HTTP/2 connection reuse to reduce new SSL connections. (c) Consider SSL offloading to a dedicated crypto accelerator VM (if available). Validation: Run traffic generator (iperf3, tcpreplay) with realistic SSL traffic mix, measure throughput, latency P99, and CPU utilization at each phase.",
      justification: "Virtual networking performance bottlenecks differ from physical. The hypervisor adds layers (vSwitch, virtio, VM exit/entry) that physical appliances avoid. DPDK and SR-IOV bypass these layers by providing direct hardware access from the VM. AES-NI provides hardware-accelerated crypto that closes the gap between software VNFs and hardware SSL accelerators.",
    },
  ],
};
