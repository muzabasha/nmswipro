import type { ActivitySolution } from "./types";

export const activitySolutions: Record<string, ActivitySolution> = {
  "u1t0": {
    level1: "The 13 key networking commands and their OSI layers are: (1) ping — sends ICMP Echo Request (Type 8) to test reachability; Layer 3 (Network). (2) traceroute/tracert — exploits TTL expiry to map each hop; Layer 3. (3) netstat — displays active TCP/UDP connections, listening ports, and routing tables; Layers 3–4 (Network/Transport). (4) ss — modern socket statistics replacing netstat; Layer 4. (5) arp — resolves IPv4 addresses to MAC addresses using ARP (RFC 826); Layer 2 (Data Link). (6) nmap — host/port discovery using TCP SYN/UDP probes; Layers 3–4. (7) dig — DNS query tool resolving domain names to IPs; Layer 7 (Application). (8) nslookup — legacy DNS lookup; Layer 7. (9) route/ip route — displays and modifies the IP routing table; Layer 3. (10) ifconfig/ip addr — configures and displays interface IP, MAC, and status; Layers 2–3. (11) tcpdump — captures and analyses live packet headers; Layers 2–7. (12) snmpget — retrieves a specific MIB OID value from an SNMP agent; Layer 7 (SNMP on UDP/161). (13) snmpwalk — walks a MIB subtree using successive GETNEXT PDUs; Layer 7. In NMS context: ping/traceroute → Fault Management; netstat/ss → Performance; arp/nmap → Configuration/Inventory; snmpget/snmpwalk → SBI polling of MIB data.",
    level2: "Expected output and interpretation: (1) ping -c 5 8.8.8.8 → 5 ICMP Echo Requests to Google's DNS. Output: 64 bytes from 8.8.8.8: icmp_seq=1 ttl=116 time=12.4 ms (×5). Final: 0% packet loss, rtt min/avg/max/mdev = 10.2/12.4/14.8/1.6 ms. Interpretation: RTT ~12 ms indicates ~6,000 km path at speed of light in fibre (100,000 km/s). Zero loss = healthy path. (2) traceroute 8.8.8.8 → Each line is one hop: hop number, IP address, RTT for 3 probes. Identify your ISP gateway (hop 1–2), ISP peering point (hop 3–5), Google backbone (hop 6+). RTT jumps identify geographic boundaries. (3) netstat -tulnp | grep LISTEN → Each line: Proto (tcp), Recv-Q (0), Send-Q (0), Local Address (0.0.0.0:22 = SSH listening on all interfaces), Foreign Address (0.0.0.0:*), PID/Program. Port 22 = SSH, 80 = HTTP, 443 = HTTPS, 53 = DNS. In NMS: this reveals which management services are exposed. (4) arp -a → Lists cached ARP mappings: hostname (192.168.1.1) at mac-addr [ether] on eth0. The gateway entry confirms Layer 2 connectivity. Incomplete entries indicate unreachable hosts. (5) ip route show → default via 192.168.1.1 dev eth0 proto dhcp metric 100 → default route; 192.168.1.0/24 dev eth0 proto kernel scope link → directly connected subnet. In NMS: routing table changes detected here indicate topology changes requiring Configuration Management updates.",
    level3: "Worked example with ping -c 100 -i 0.5 192.168.1.1: Suppose output shows: 100 packets transmitted, 87 received, 13% packet loss, rtt min/avg/max/mdev = 2.1/18.4/312.5/45.2 ms. Step (a) Packet Loss % = (100−87)/100 × 100 = 13%. Step (b) CV = mdev/avg = 45.2/18.4 = 2.46. A CV > 0.3 strongly indicates congestion (bursty loss) rather than uniform random bit errors. Step (c) Queue depth: delta_RTT = avg − min = 18.4 − 2.1 = 16.3 ms. queue_bytes = delta_RTT × bandwidth / 8 = 0.0163 s × 100×10⁶ bits/s / 8 = 203,750 bytes ≈ 200 KB. This is the estimated instantaneous queue depth on the bottleneck link. Interpretation: CV = 2.46 >> 0.3 → congestion is the root cause. Action: escalate to capacity team for WAN bandwidth upgrade. If CV were < 0.1 with similar loss, the diagnosis would be physical layer bit errors, and you would request BER measurement from the carrier.",
    level4: "A complete bash health-check script targeting a list of 5 IPs: #!/bin/bash. TARGET_IPS=(\"192.168.1.1\" \"192.168.1.2\" \"10.0.0.1\" \"10.0.0.2\" \"172.16.0.1\"). echo '[' > report.json. for IP in ${TARGET_IPS[@]}; do PING_OUT=$(ping -c 10 -q $IP 2>&1); REACHABLE=$(echo $PING_OUT | grep -c '0% packet loss' > 0 && echo true || echo false); LOSS=$(echo $PING_OUT | grep -oP '[0-9]+(?=% packet loss)'); LAST_HOP=$(traceroute -m 15 -q 1 $IP 2>/dev/null | tail -2 | head -1 | awk '{print $2}'); ESTABLISHED=$(netstat -tn | grep ESTABLISHED | grep -c $IP); PORTS=$(nmap -sV --open -oG - $IP 2>/dev/null | grep Ports | sed 's/.*Ports: //'); echo "{\"ip\":\"$IP\",\"reachable\":$REACHABLE,\"loss_percent\":$LOSS,\"last_hop\":\"$LAST_HOP\",\"established_connections\":$ESTABLISHED,\"open_ports\":\"$PORTS\"}" >> report.json; done. echo ']' >> report.json. This produces a structured JSON array ready for ingestion by an NMS probing module. The script covers: ICMP reachability (ping) → Fault Management, path tracing (traceroute) → Topology Discovery, connection state (netstat) → Performance Management, service enumeration (nmap) → Configuration Inventory — all four NMS FCAPS domains automated in one script."
  },
  "u1t1": {
    level1: "The four main mobile network components are: (1) UE (User Equipment) — the end-user device (smartphone, IoT sensor, laptop). Its primary function is to originate and terminate user data and control signalling. It communicates with the RAN via the air interface using licensed spectrum (LTE: 700 MHz–2.6 GHz; 5G NR: sub-6 GHz and mmWave). (2) RAN (Radio Access Network) — comprising eNBs (4G) or gNBs (5G) base stations that manage the wireless air interface. Primary function: radio resource management, modulation/coding, scheduling, and handovers. Communicates with Core via S1 interface (4G) or NG interface (5G). (3) Core Network — in 4G: EPC (MME for mobility/authentication, S-GW for user plane routing, P-GW for IP address assignment and internet breakout, HSS for subscriber database). In 5G: 5GC (AMF for access and mobility, SMF for session management, UPF for user plane, UDM for unified data management). Primary function: IP connectivity, authentication, session management, policy enforcement, and internet interconnection. (4) Management Plane — the NMS/OSS layer sitting above all other planes. Primary function: collect KPIs, alarms, and configuration data from all RAN and Core elements; enable operators to monitor, configure, and troubleshoot the entire network from a central console. In FCAPS terms: it implements all five management functions across all network layers.",
    level2: "A voice call from UE to PSTN travels as follows — label each interface: UE ─[Uu air interface]─ eNB/gNB (RAN): The UE sends uplink radio frames. The eNB demodulates the signal, reassembles IP packets, and forwards voice packets over the S1-U (user plane) interface. eNB ─[S1-MME control]─ MME (4G Core): The MME authenticates the UE (via HSS), manages mobility (handovers), and establishes the bearer. eNB ─[S1-U]─ S-GW: User plane packets flow through the Serving Gateway. S-GW ─[S5/S8]─ P-GW: The Packet Gateway assigns an IP address and provides NAT/internet access. P-GW ─[SGi]─ Internet/IMS: For VoIP calls, packets reach the IMS (IP Multimedia Subsystem). IMS ─[PSTN Gateway]─ PSTN: The IMS converts VoIP to ISDN/SS7 for delivery to the fixed telephone network. Key labels: Uu (air interface, radio frames), S1-MME (control, RRC/NAS), S1-U (user plane, GTP-U tunnelled UDP/IP), S5 (S-GW to P-GW), SGi (P-GW to internet), Gm/Mw (IMS interfaces). In 5G: replace eNB→gNB, MME→AMF, S-GW+P-GW→UPF, S1→NG1/NG-U, SGi→N6.",
    level3: "Shannon capacity calculation: C = B × log₂(1 + SNR). Given: B = 20 MHz = 20×10⁶ Hz, SNR = 15 dB. Step 1 — Convert SNR: SNR_linear = 10^(15/10) = 10^1.5 = 31.62. Step 2 — Apply Shannon: C = 20×10⁶ × log₂(1 + 31.62) = 20×10⁶ × log₂(32.62). Step 3 — Compute log₂(32.62): log₂(32.62) = ln(32.62)/ln(2) = 3.485/0.693 = 5.031 bits/s/Hz. Step 4 — Final: C = 20×10⁶ × 5.031 = 100.62 Mbps. This is the theoretical Shannon limit. In practice, LTE achieves 60–75% of this (≈60–75 Mbps) due to overhead, guard bands, and practical modulation limits (256-QAM achieves ≈8 bits/symbol vs Shannon's 5.03 bits/s/Hz here). For the NMS, this capacity figure is used as the baseline KPI — if monitored throughput falls below 50% of Shannon capacity, it signals suboptimal radio conditions requiring performance management action.",
    level4: "4G LTE vs 5G NR comparison: Architecture — 4G uses EPC (monolithic core with MME, S-GW, P-GW as separate physical nodes). 5G uses 5GC (cloud-native microservices: AMF, SMF, UPF, AUSF, UDM, PCF, NRF — each independently scalable, communicating via HTTP/2 service-based interfaces). RAN — 4G: eNB (monolithic base station handling all radio and baseband processing). 5G NR: gNB split into CU (Central Unit) and DU (Distributed Unit) with F1 interface; CU further split into CU-CP (control plane) and CU-UP (user plane). Latency targets — 4G: ~30–50 ms end-to-end. 5G eMBB: <10 ms; URLLC: <1 ms. Spectrum — 4G: sub-6 GHz (FDD/TDD). 5G NR: sub-6 GHz + mmWave (24–100 GHz, beamforming essential). Management implications — 5G's service-based architecture exposes management APIs as REST/HTTP (unlike 4G's proprietary NBI), enabling intent-based management. 5G O-RAN disaggregation creates multi-vendor RAN requiring standardised NMS interfaces (O1, O2, A1). 5G network slicing (eMBB, URLLC, mMTC) requires slice-aware FCAPS — separate fault, performance, and SLA management per slice. These changes demand NMS platforms supporting NETCONF/YANG models aligned with 3GPP TS 28.532 (management services) and O-RAN Alliance specifications."
  },
  "u1t2": {
    level1: "The 5-layer TMN pyramid with labels: Layer 5 (top) — Business Management Layer (BML): Handles billing, SLA management, business planning, revenue assurance. Real-world system: BSS/billing platform (e.g., Amdocs). Interface to NML: X interface (inter-domain) or proprietary NBI. Layer 4 — Network Management Layer (NML): End-to-end cross-domain view, network-wide KPI aggregation, cross-domain service correlation. Real-world system: Nokia NetAct, Ericsson ENM. Interface to EML: Q3 northbound; to BML: X or proprietary. Layer 3 — Element Management Layer (EML): Single-vendor/technology domain management, alarm aggregation, domain-level correlation. Real-world system: Ericsson ENM per-domain, Huawei U2000 LTE. Interface to NEL: Q3 (SNMP/NETCONF); to NML: northbound API. Layer 2 — Network Element Layer (NEL): The actual physical or virtual network devices (base stations, routers, switches). Each NE runs an SNMP agent or NETCONF server. Interface to EML: Q3 (SNMP GET/SET on UDP 161, NETCONF on TCP 830). Layer 1 (base) — Q Adaptor Layer (QAL): Protocol adapters for legacy NEs that cannot speak standard Q3. Converts proprietary CLI/TL1 to standards-based Q3. Used for legacy SONET/SDH equipment. Interface: vendor-specific southward, Q3 northward.",
    level2: "FCAPS to TMN layer and eTOM area mapping: Fault Management → primary at EML (first alarm detection, domain correlation) and NML (cross-domain root-cause). eTOM area: Operations → Assurance (Trouble & Problem Management). Configuration Management → EML (per-device provisioning, backup) and NML (end-to-end service provisioning). eTOM: Operations → Fulfillment (Service Configuration & Activation) + Assurance (Configuration Audit). Accounting Management → NML and BML (CDR aggregation, billing input). eTOM: Operations → Billing (Mediation & Billing) + Fulfillment (Order Billing). Performance Management → EML (per-NE counter collection) and NML (KPI aggregation, threshold alerting). eTOM: Operations → Assurance (QoS Management, SLA Management). Security Management → all layers: EML (AAA for device access), NML (RBAC for NMS users), BML (audit, compliance). eTOM: Enterprise Management (Security Management process area, cross-cutting all OPS processes).",
    level3: "Management overhead calculation: Formula: M = (N / C) × T. Given: N = 200 NEs, C = 20 (concurrent polls), T = 5 seconds per element. M = (200 / 20) × 5 = 10 × 5 = 50 seconds. This means the EMS completes one full polling cycle in 50 seconds, which comfortably fits within a 5-minute (300-second) polling interval with a 5:1 headroom margin. To reduce M to under 60 seconds: the constraint is already satisfied (50 < 60). If the question was with N = 200, C = 10 (halved): M = (200/10) × 5 = 100 seconds — exceeds 60 seconds. Required concurrency: C_min = (N × T) / M_target = (200 × 5) / 60 = 1000/60 = 16.67 → C = 17. Thus the EMS needs at least 17 concurrent polling threads to achieve a sub-60-second cycle for 200 NEs at 5 seconds per poll. In practice, EMS platforms use thread pools with 2× the calculated minimum to handle retry/timeout overhead.",
    level4: "Ericsson ENM (Evolved Network Manager) eTOM implementation: Ericsson ENM implements the NML layer of the TMN hierarchy for Ericsson RAN and Core domains. eTOM process groups supported: (1) Assurance: ENM's Fault Management module receives SNMP traps and NETCONF notifications from eNB/gNB, provides root-cause alarm correlation across RAN cells, and exports correlated alarms via REST NBI to the operator's OSS ticketing system. Performance Management module collects PM counters (PM files in 3GPP XML format via SFTP) every 15 minutes and exposes KPIs on dashboards and threshold alerts. (2) Fulfillment: ENM's Configuration Management module supports bulk node provisioning via NETCONF/YANG and provides a Zero-Touch Provisioning (ZTP) workflow for new gNB onboarding. Network Design & Assign sub-process uses ENM for radio parameter planning and cell configuration. (3) Billing: ENM does not directly handle billing, but provides CDR mediation feeds to the BSS via REST API. The NBI (ENM REST API) exposes endpoints for alarm retrieval (aligned with 3GPP IRP TS 32.342), PM data retrieval, and configuration push — mapping directly to the eTOM Operations Support & Readiness processes."
  },
  "u1t3": {
    level1: "The four-tier management hierarchy with interface labels: Tier 4 (top) — OSS/BSS (Operations/Business Support System): Service-level management, customer-facing portals, billing, SLA dashboards. Interface to NMS: NBI (Northbound Interface) — REST/HTTPS JSON, TMF OpenAPI (e.g., TMF642 Fault Management API). Tier 3 — NMS (Network Management System): Multi-vendor, multi-domain end-to-end network view. Cross-domain alarm correlation, topology aggregation, performance trending. Interface to EMS: SBI (Southbound Interface) — SNMP, NETCONF, vendor REST APIs; to OSS: NBI. Tier 2 — EMS (Element Management System): Single-vendor domain management (e.g., Ericsson ENM for 4G, Nokia NetAct for 5G). Vendor-specific deep management: SON, advanced PM, bulk provisioning. Interface to NE: Q3 (SNMP on UDP/161, NETCONF on TCP/830); to NMS: northbound proprietary or REST API. Tier 1 (bottom) — NE (Network Element): Physical/virtual devices: base stations (eNB, gNB), routers, switches, firewalls. Runs SNMP Agent (UDP 161/162) or NETCONF server (TCP 830). Interface to EMS: Q3. In summary: Q3 goes device→EMS, SBI goes EMS→NMS, NBI goes NMS→OSS/BSS.",
    level2: "NBI (Northbound Interface): Connects the NMS upward to OSS/BSS platforms and third-party applications. Systems connected: NMS → OSS (Operations Support System), BSS (Business Support System), analytics engines, ITSM platforms (ServiceNow). Protocols used: REST/HTTPS with JSON (primary for modern platforms), TMF OpenAPIs (TMF642 Fault, TMF628 Performance, TMF641 Service Configuration), SOAP/XML (legacy telecom OSS), CORBA/CMIP (very old platforms). Data flows: The NMS sends correlated alarm feeds, network topology graphs, aggregated KPI data, and configuration status upward. The OSS sends service provisioning requests and maintenance windows downward. SBI (Southbound Interface): Connects the NMS downward to EMS domains and, in some cases, directly to NEs. Systems connected: NMS → EMS (Ericsson ENM, Nokia NetAct, Huawei U2000) and NMS → NEs (in agentless deployments). Protocols used: SNMP v2c/v3 (polling + traps from legacy EMS or direct NEs), NETCONF/YANG (RFC 6241, configuration management), gRPC/gNMI (OpenConfig streaming telemetry), vendor REST APIs (some EMSs expose REST northbound). Data flows: The NMS polls performance counters and topology state downward via GET; EMS sends alarms as SNMP traps or NETCONF notifications upward; NMS pushes configuration changes downward via NETCONF edit-config or REST POST.",
    level3: "Alarm Aggregation Ratio (R_agg) calculation: Total raw alarms = 5 EMS domains × 80 raw alarms = 400 raw alarms. Correlated root-cause alarms at NMS = 60. R_agg = Total Raw / Correlated = 400 / 60 ≈ 6.67. Interpretation: A ratio of 6.67 means the operator sees 1 actionable alarm for every 6–7 raw device events. This is a good aggregation ratio — industry best practice targets R_agg > 5 to prevent alarm fatigue in NOC operations. A NOC shift with 400 raw alarms in 15 minutes is operationally paralysing; 60 root-cause alarms is manageable. Analysis: Each raw alarm beyond the correlated set is a derivative/symptomatic alarm — e.g., one core switch failure causing 60 downstream linkDown traps, which the NMS correlates into a single root-cause incident. To further improve: increase correlation rule coverage (pattern-based grouping, topology-aware propagation), add time-window correlation to group alarms occurring within 30 seconds of each other, and implement root-cause scoring to rank the most probable single failure point.",
    level4: "Research outcome — Nokia NetAct NMS architecture: Nokia NetAct is a commercial NMS operating at the TMN NML tier. Supported EMS domains: NetAct natively integrates with Nokia EMS for 4G LTE RAN (RL60/RL70 releases) and 5G NR, and uses mediation adapters for third-party vendors (Ericsson, Huawei) via SNMP/NETCONF SBI. Total scale: supports up to 500,000 managed objects in large deployments. NBI interfaces exposed: (1) Northbound REST API following 3GPP IRP standards (TS 32.342 for fault, TS 32.432 for performance); (2) CORBA/CMIP interface for legacy OSS integration; (3) XML file-based PM data export via SFTP (3GPP PM XML format); (4) Kafka streaming interface for real-time alarm feeds to analytics platforms. Alarm correlation: NetAct implements rule-based alarm correlation using its Alarm Consolidation Framework — rules are defined in XML and include temporal windowing, topological dependency, and suppression policies. A single core node failure generating 500 derivative alarms is consolidated to 1 root-cause alarm in under 5 seconds. Configuration management: Uses NETCONF/YANG for 5G gNB provisioning and proprietary XML-over-SOAP for 4G legacy provisioning, demonstrating the mixed SBI approach common in multi-generation networks."
  },
  "u1t4": {
    level1: "FCAPS definitions with mobile network operator examples: F — Fault Management: Detect, isolate, and correct abnormal conditions in managed objects. Daily operations example: An alarm appears on the NMS dashboard showing 'eNB-042: S1-MME link down' at 02:15 AM. The NOC engineer acknowledges the alarm, checks the EMS for related interface errors, dispatches a field engineer to inspect the S1 fibre connection, and closes the ticket after restoring connectivity. NMS tool: Alarm Management Screen (real-time alarm table, correlated view). C — Configuration Management: Maintain accurate records of system configurations and control changes. Daily operations example: Adding 5 new cells to a sector — the RAN planning team creates a change request, submits it to the CMDB, the provisioning system pushes the cell configuration via NETCONF to the gNB, and a post-change audit confirms the running config matches the planned config. NMS tool: Configuration Management module (CMDB, change management workflow). A — Accounting Management: Collect and process data for billing and resource usage tracking. Daily operations example: Every 15 minutes, the Core network generates CDRs (Call Detail Records) for each data session. The mediation server parses CDRs, aggregates by subscriber, and feeds the billing system. Quota enforcement triggers throttling when a prepaid subscriber's data balance reaches zero. NMS tool: Mediation server, charging system (OCS/OFCS). P — Performance Management: Monitor KPIs to ensure quality of service. Daily operations example: The NOC receives an automated alert — 'Cell CID-1045: average user throughput < 2 Mbps for 30 minutes'. The engineer checks the PM dashboard, identifies rising interference from a neighbouring cell, and adjusts the antenna tilt remotely via the EMS. NMS tool: KPI Dashboard, PM counter viewer (e.g., Ericsson ENM Performance Manager). S — Security Management: Protect network infrastructure and customer data. Daily operations example: The security system detects 15 failed SSH login attempts to a core router within 2 minutes from an unknown IP. The NMS triggers an automatic ACL block on the management plane firewall and raises a Critical security alarm. Audit logs capture the exact timestamps and source IPs for incident analysis. NMS tool: Security audit module, AAA/RADIUS integration, intrusion detection dashboard.",
    level2: "FCAPS response timeline for 'Core router CR-01 loses power at 10:00 AM': 10:00:00 AM — SNMP Agent on CR-01 goes silent. Pending polls time out. FAULT MANAGEMENT activated: NMS detects missing poll responses within 3 poll cycles (3 × 60s = 3 minutes). At 10:03 AM, NMS raises Critical alarm 'CR-01: Node Unreachable'. Alarm correlation engine groups the 5 downstream switch linkDown alarms into one root-cause incident pointing to CR-01 power failure. Auto-ticket created in ServiceNow. NOC engineer acknowledged at 10:04 AM. CONFIGURATION MANAGEMENT actions (10:04–10:15 AM): NMS identifies CR-01 as the failure point from topology data. CMDB lookup shows CR-01 carries OSPF area 0 backbone traffic. The engineer initiates an emergency configuration change — activating the backup static routes on CR-02 and CR-03 to restore partial connectivity for the 200 affected users. Change record created and approved within 5 minutes using pre-approved break-glass procedure. PERFORMANCE MANAGEMENT (10:03–12:00 PM): PM counters from surviving nodes show elevated throughput on backup paths (utilisation rises from 40% to 85%). NMS alerts on interface utilisation threshold (80%) — NOC monitors for congestion. MTTR clock starts. ACCOUNTING MANAGEMENT (10:03 AM onwards): Session records for 200 users affected. The billing system logs the outage window for CDR auditing. SLA breach detected: 13 enterprise customers on 99.99% SLA agreements have downtime records created for SLA credit calculations. SECURITY MANAGEMENT (throughout): No SSH access to CR-01 (device is down). Post-incident security review verifies no unauthorised access preceded the power failure. Access logs for the 2 hours before the fault are archived for forensic analysis. 12:00 PM — CR-01 restored (power module replaced). SNMP agent comes back online. NMS detects warm-start trap. Engineer reconfigures via NETCONF, verifies configuration matches CMDB baseline. Alarm cleared. Ticket closed.",
    level3: "Network availability and MTTR calculation: Given MTBF = 5000 hours, MTTR = 2 hours. A = MTBF / (MTBF + MTTR) × 100% = 5000 / (5000 + 2) × 100% = 5000 / 5002 × 100% = 99.9600% (to 4 decimal places). This is approximately 3-nines availability with 35 minutes of annual downtime. Now solve for MTTR to achieve 99.999% (5-nines): 0.99999 = MTBF / (MTBF + MTTR) → MTBF + MTTR = MTBF / 0.99999 → MTTR = MTBF × (1/0.99999 − 1) = 5000 × (1.00001 − 1) = 5000 × 0.00001 = 0.05 hours = 3 minutes. To achieve 5-nines availability with MTBF = 5000 hours, the MTTR must be no more than 3 minutes. This demonstrates why 5-nines networks require automated fault detection and pre-scripted remediation playbooks — a human response time of 15–30 minutes is far too slow. Automated systems must detect, diagnose, and remediate in under 3 minutes. This is the quantitative justification for FCAPS automation in NMS platforms.",
    level4: "Complete FCAPS management plan for a university campus network (50 access switches, 5 distribution routers, 1 Gbps internet uplink): FAULT MANAGEMENT — KPIs: device availability (target 99.9%), alarm MTTR (target < 30 min). Tools: LibreNMS or Zabbix for SNMP polling every 60 seconds; PagerDuty for on-call alerting. Escalation: Critical alarms page the on-call engineer immediately; Major alarms email the team; Minor alarms logged for morning review. Automation rule: if a switch goes down and is in the dependency tree of 10+ APs, auto-create a P1 ticket and send SMS to the network team. CONFIGURATION MANAGEMENT — KPIs: configuration compliance rate (target 100%), unauthorised changes detected within 15 minutes. Tools: RANCID or Oxidized for daily config backups via SSH; CMDB in spreadsheet or GLPI. Escalation: Any config diff from baseline triggers an email alert; changes outside the approved change window trigger a P2 incident. Automation rule: schedule nightly RANCID backup jobs; diff against previous day's backup. ACCOUNTING MANAGEMENT — KPIs: internet uplink utilisation (daily peak), top 5 bandwidth consumers per building. Tools: NetFlow collector (ntopng free edition) + MRTG for SNMP-based traffic graphs. Escalation: If a single host exceeds 200 Mbps sustained for 5 minutes, alert NOC (possible P2P or streaming abuse). Automation: auto-block known BitTorrent ports exceeding 100 Mbps. PERFORMANCE MANAGEMENT — KPIs: uplink utilisation (target < 80% at peak), latency to internet gateway (target < 10 ms), packet loss (target < 0.1%). Tools: Cacti or Grafana + Prometheus SNMP exporter for real-time dashboards. Escalation: 80% uplink utilisation → Warning alert; 95% → Critical alert, request ISP bandwidth upgrade. Automation rule: if latency exceeds 100 ms for 5 consecutive polls, trigger traceroute to identify the bottleneck hop. SECURITY MANAGEMENT — KPIs: failed login attempts per device per hour (threshold 5), CVE patch compliance rate (target 100% within 30 days). Tools: Syslog server (rsyslog) + Graylog for log aggregation; vulnerability scanner (OpenVAS). Escalation: > 5 failed SSH attempts in 2 minutes → auto-block IP on perimeter firewall. Automation rule: weekly scan for default community strings ('public'/'private'); alert if found, auto-rotate if possible."
  },
  "u1t5": {
    level1: "NMS architecture diagram with SBI and NBI labelled: The diagram shows three horizontal tiers. Top tier (OSS/BSS Applications): Service portals, ticketing systems (ServiceNow), analytics platforms, billing systems. Connected downward to NMS via the NBI arrow labelled 'NBI (Northbound Interface)'. Middle tier (NMS): The central platform providing cross-domain visibility, alarm correlation, topology aggregation, and performance trending. Connected downward to EMS/device layer via the SBI arrow labelled 'SBI (Southbound Interface)'. Bottom tier (Managed Devices): Three device types shown side by side — Router (SNMP v2c/v3), Switch (NETCONF), 5G gNB (gNMI). Directly below the NMS pointing to devices. SBI annotation: Three protocol labels on the SBI arrow going down — SNMPv3 (legacy devices), NETCONF/YANG (RFC 6241, config devices), gRPC/gNMI (OpenConfig, streaming telemetry). NBI annotation: Two protocol labels on the NBI arrow going up — REST/HTTPS + JSON (modern OSS), TMF OpenAPI (TMF642 Fault, TMF628 Performance). Communication direction: SBI points DOWNWARD toward devices; NBI points UPWARD toward OSS/BSS applications. This separation is fundamental — the SBI is device-protocol-aware (speaking each device's native language), while the NBI presents a unified, abstracted service-level API to all consumers.",
    level2: "SNMP (SBI) vs REST API (NBI) comparison table: Dimension 1 — Data model: SNMP uses MIB OID tree (hierarchical numeric identifiers, e.g., 1.3.6.1.2.1.2.2.1.10 for ifInOctets); REST NBI uses JSON schema with human-readable field names (e.g., {\"alarm\":{\"severity\":\"Critical\",\"source\":\"eNB-042\"}}). Dimension 2 — Security: SNMP v1/v2c uses cleartext community strings (fundamentally insecure); SNMP v3 adds HMAC-SHA auth + AES-128 encryption. REST NBI uses OAuth2 tokens + TLS 1.3 transport encryption (standard web security stack). Dimension 3 — Real-time capability: SNMP is primarily polling-based (NMS sends GET every N seconds, latency = polling interval); SNMP TRAPs are event-driven but UDP fire-and-forget. REST NBI is stateless request-response (not push); modern NBIs add WebSocket or SSE for streaming. Dimension 4 — Primary consumer: SNMP SBI is used exclusively by the NMS internally to collect raw device data; the NMS operator never directly queries SNMP in normal operations. REST NBI is used by external application developers, OSS vendors, ticketing systems, and analytics platforms — it is the external-facing API. Summary: the SBI is optimised for machine-to-machine reliability in a constrained management network; the NBI is optimised for developer accessibility, security boundary enforcement, and data abstraction.",
    level3: "Poll cycle time calculation: Formula: T_poll = (N × S) / B. Given: N = 500 devices, S = 800 bytes PDU size, B = 2 Mbps = 2,000,000 bits/s = 250,000 bytes/s. T_poll = (500 × 800) / 250,000 = 400,000 / 250,000 = 1.6 seconds. This easily fits within a 15-minute (900-second) monitoring interval, leaving 898.4 seconds of headroom. Decision: the 2 Mbps SBI bandwidth is sufficient for 500 devices with 800-byte PDUs at 15-minute intervals. Now check if the interval would be violated: T_poll = 1.6 s << 900 s — confirmed adequate. Minimum bandwidth needed to poll within 60 seconds (tighter SLA): B_min = (N × S) / T_target = (500 × 800) / 60 = 400,000 / 60 = 6,667 bytes/s = 53.3 kbps. Even a 1 Mbps link can support 500 devices at 60-second polling. The formula shows that for 15-minute polling, even a 56 kbps modem-speed link could theoretically serve 500 devices — the binding constraint is not bandwidth but rather latency when concurrent polling creates packet bursts.",
    level4: "NBI REST API design for three endpoints: Endpoint 1: GET /alarms — Returns all currently active alarms. HTTP Method: GET. URL: https://nms.example.com/api/v1/alarms?severity=Critical&limit=100. Request parameters: severity (optional filter), source (device IP), limit (pagination). Sample JSON response: {\"alarms\": [{\"id\": \"ALM-20240715-001\", \"severity\": \"Critical\", \"source\": \"192.168.1.1\", \"description\": \"eNB-042 S1 link down\", \"raised_at\": \"2024-07-15T10:03:00Z\", \"acknowledged\": false}], \"total\": 1, \"page\": 1}. Endpoint 2: GET /topology — Returns the network graph. HTTP Method: GET. URL: https://nms.example.com/api/v1/topology?format=json. Sample JSON response: {\"nodes\": [{\"id\": \"CR-01\", \"type\": \"router\", \"ip\": \"10.0.0.1\", \"status\": \"up\"}], \"links\": [{\"source\": \"CR-01\", \"target\": \"SW-01\", \"bandwidth_mbps\": 1000, \"utilisation_pct\": 45}]}. Endpoint 3: POST /config — Pushes a configuration change to a device via NMS. HTTP Method: POST. URL: https://nms.example.com/api/v1/config. Request body: {\"device_id\": \"eNB-042\", \"change_type\": \"parameter_update\", \"parameters\": {\"tx_power_dbm\": 43, \"cell_id\": 1045}}. Sample response: {\"status\": \"queued\", \"job_id\": \"CFG-20240715-007\", \"estimated_completion\": \"2024-07-15T10:15:00Z\"}. Authentication: All endpoints require Bearer token in Authorization header. Rate limiting: 100 requests/minute per client IP. Error responses: 401 Unauthorized, 403 Forbidden (RBAC), 404 Not Found, 429 Too Many Requests."
  },
  "u1t6": {
    level1: "SNMP version comparison table: SNMPv1 (RFC 1157, 1988): Key new feature — established the foundational GET, GETNEXT, SET, TRAP PDU types and the Manager-Agent model. Security limitation — community strings transmitted in cleartext; no authentication, no encryption; TRAP is unacknowledged UDP fire-and-forget. RFC: RFC 1157. SNMPv2c (RFC 1901 + RFC 3416, 1993): Key new feature — added GETBULK (retrieves N rows per PDU, dramatically reducing table walk PDU count) and INFORM (acknowledged notification with retransmission). Also added 64-bit Counter64 for high-speed interface counters that Counter32 overflows every 34 seconds at Gbps rates. Security limitation — retained cleartext community strings ('c' = community-based); the SNMPv2u/party-based security attempts were abandoned due to complexity. RFC: RFC 1901, 3416. SNMPv3 (RFC 3414 + RFC 3415, 1999/2004): Key new feature — introduced the User-based Security Model (USM): HMAC-MD5 or HMAC-SHA authentication (proves sender identity and detects tampering), DES-CBC or AES-128-CFB encryption (confidentiality), and 150-second replay-attack window. Also added View-based Access Control Model (VACM): per-user, per-MIB-subtree read/write/notify access control — replacing coarse community string granularity. Security limitation addressed — cleartext credentials eliminated. Remaining limitation: still uses UDP (connectionless), still polling-based, no transaction semantics. RFC: RFC 3414 (USM), RFC 3415 (VACM).",
    level2: "MIB OID hierarchy explanation for sysDescr: The full path is: iso(1) → org(3) → dod(6) → internet(1) → mgmt(2) → mib-2(1) → system(1) → sysDescr(1) → instance(0). Numeric OID: 1.3.6.1.2.1.1.1.0. Breakdown of each arc: iso(1) — the root assigned by ISO. org(3) — organizational branch. dod(6) — US Department of Defense, which initially funded ARPANET. internet(1) — the Internet subtree. mgmt(2) — IETF-managed objects. mib-2(1) — the standard MIB-II (RFC 1213). system(1) — the system group, containing general device information. sysDescr(1) — the first object in the system group: a human-readable description of the device hardware and operating system (e.g., 'Cisco IOS Software, Version 15.4...'). instance(0) — the instance identifier .0 is mandatory for all SCALAR objects in SNMP. It distinguishes the single instance of a scalar from a table row. sysDescr contains: a free-form string (up to 255 characters) describing the device's hardware type, firmware version, and vendor. It is SYNTAX DisplayString, MAX-ACCESS read-only (cannot be SET), STATUS current (active in current standard). The .0 suffix: scalar objects (not part of a table) have exactly one instance, identified by appending .0. Without .0, the OID refers to the object type definition, not an instance — GET would fail with noSuchInstance.",
    level3: "SNMP overhead fraction calculation: Formula: O_snmp = (N × V × S) / (Δt × B). Given: N = 200 nodes, V = 15 OID variables per node, S = 250 bytes PDU size, Δt = 60 seconds, B = 125,000 bytes/s (1 Mbps). Step 1 — Numerator (total bytes per poll cycle): 200 × 15 × 250 = 750,000 bytes. Step 2 — Denominator (bytes available in Δt): 60 × 125,000 = 7,500,000 bytes. Step 3 — Overhead fraction: O_snmp = 750,000 / 7,500,000 = 0.10 = 10%. Verdict: 10% SNMP overhead is ABOVE the 5% guideline. The management link is at risk of saturation if other management traffic (syslog, NETCONF, SSH) is also present. To satisfy the 5% guideline at this node count: either increase Δt to 120 seconds (O = 5%), or increase bandwidth to 2 Mbps (O = 5%), or reduce V from 15 to 7 variables per node (O ≈ 4.67%). Practical recommendation: increase polling interval to 120 seconds for non-critical counters, retain 60-second polling only for fault-critical KPIs, and rely on SNMP TRAPs for real-time fault detection to reduce polling dependency.",
    level4: "GNS3/Packet Tracer SNMP configuration lab: Step 1 — Configure SNMP community on Cisco IOS router: Router(config)# snmp-server community public RO. Router(config)# snmp-server community private RW. Router(config)# snmp-server host 192.168.1.100 version 2c public. Router(config)# snmp-server enable traps. Step 2 — Retrieve sysUpTime from CLI: Command: snmpget -v2c -c public 192.168.1.1 1.3.6.1.2.1.1.3.0. Expected output: DISMAN-EVENT-MIB::sysUpTimeInstance = Timeticks: (284562) 0:47:25.62. Interpretation: The device has been running for 47 minutes and 25.62 seconds. The Timeticks value is in hundredths of a second (284562 / 100 = 2845.62 seconds = 47 min 25.62 sec). Step 3 — Verify trap receiver: Use snmptrapd -v2c -c public -Lo on the 'NMS' host. Trigger an interface shutdown on the router: Router(config)# interface g0/0. Router(config-if)# shutdown. The trapd process should receive: 2024-07-15 10:30:00 192.168.1.1: Trap 1.3.6.1.6.3.1.1.5.3 — linkDown notification. The snmpTrapOID.0 = 1.3.6.1.6.3.1.1.5.3 (linkDown). sysUpTime.0 = the uptime at the moment of the event. This exercise demonstrates that SNMP TRAP delivery requires: (a) correct trap receiver IP configured on the device, (b) correct community string matching the receiver's expectation, (c) UDP connectivity from device to manager on port 162."
  },
  "u1t7": {
    level1: "Complete SNMP Manager-Agent architecture diagram description: Components to draw: (1) NMS Manager Host (left box): Contains the SNMP Manager application, trap receiver daemon (UDP port 162), MIB browser, and polling scheduler. (2) Network Device with SNMP Agent (right box): Contains the SNMP Agent process, the local MIB module (read/write access to device data), and listens on UDP port 161 for incoming requests. (3) MIB inside Agent: Hierarchical tree showing the data the Agent manages — e.g., system group (sysUpTime, sysName) and interfaces table (ifDescr, ifSpeed, ifOperStatus). PDU arrows with directions — all sent over UDP/IP: GET → (Manager to Agent, port 161): 'Give me the value of OID 1.3.6.1.2.1.1.3.0'. GETNEXT → (Manager to Agent, port 161): 'Give me the next OID after this one'. GETBULK → (Manager to Agent, port 161): 'Give me the next N rows from this OID onwards'. SET → (Manager to Agent, port 161): 'Write this value to this OID'. RESPONSE ← (Agent to Manager, responding to Manager's source port): 'Here is the answer to your request'. TRAP → (Agent to Manager, port 162): Unsolicited — 'A critical event just occurred'. Label UDP port 161 on the Agent side (receiving GET/SET/GETBULK from Manager). Label UDP port 162 on the Manager's trap receiver (receiving TRAP from Agent). The Manager initiates all GETs; the Agent initiates TRAPs only.",
    level2: "MIB-II OID paths for three objects: (a) sysUpTime: Full OID path: iso(1).org(3).dod(6).internet(1).mgmt(2).mib-2(1).system(1).sysUpTime(3).0. Numeric: 1.3.6.1.2.1.1.3.0. MIB group: system group. Type: SCALAR — one single instance for the whole device, denoted by the .0 instance suffix. Value: time in hundredths of seconds since last reboot. (b) ifOperStatus: Full OID path: iso(1).org(3).dod(6).internet(1).mgmt(2).mib-2(1).interfaces(2).ifTable(2).ifEntry(1).ifOperStatus(8).{ifIndex}. Numeric: 1.3.6.1.2.1.2.2.1.8.{N} where N = interface index (e.g., .1 for first interface). MIB group: interfaces group. Type: TABULAR — one row per interface, keyed by ifIndex. Values: 1=up, 2=down, 3=testing, 4=unknown, 5=dormant, 6=notPresent, 7=lowerLayerDown. (c) ipInReceives: Full OID path: iso(1).org(3).dod(6).internet(1).mgmt(2).mib-2(1).ip(4).ipInReceives(3).0. Numeric: 1.3.6.1.2.1.4.3.0. MIB group: ip group. Type: SCALAR (one counter for the whole device, not per-interface). Value: total number of IP datagrams received at the IP layer (Counter32 — wraps at 2³²-1 ≈ 4.3 billion).",
    level3: "V_max (max varbinds per PDU) calculation: Formula: V_max = ⌊(MTU − H_udp − H_snmp) / S_varbind⌋. Given: MTU = 1500 bytes, H_udp = 28 bytes (20 IP + 8 UDP), H_snmp = 50 bytes, S_varbind = 35 bytes. Step 1 — Available payload: 1500 − 28 − 50 = 1422 bytes. Step 2 — V_max: ⌊1422 / 35⌋ = ⌊40.63⌋ = 40 varbinds per PDU. Standard Ethernet MTU result: V_max = 40. Now for jumbo frame MTU = 9000 bytes: Available payload: 9000 − 28 − 50 = 8922 bytes. V_max: ⌊8922 / 35⌋ = ⌊254.91⌋ = 254 varbinds per PDU. Ratio of improvement: 254 / 40 = 6.35×. Jumbo frames provide 6.35× more varbinds per PDU. For a 500-row ifTable: With V_max=40 (standard): PDUs needed = ⌈500/40⌉ = 13. With V_max=254 (jumbo): PDUs needed = ⌈500/254⌉ = 2. This 6.5× reduction in round trips (from 13 to 2 PDUs) translates directly to a 6.5× faster polling cycle, which is why enabling jumbo frames on the management network is a high-ROI optimisation for large NMS deployments.",
    level4: "snmpbulkwalk command and output documentation: Command to walk full ifTable with max-repetitions=20: snmpbulkwalk -v2c -c public -Cn0 -Cr20 192.168.1.1 1.3.6.1.2.1.2.2. Sample output for five ifTable columns across two interfaces: (1) IF-MIB::ifIndex.1 = INTEGER: 1 — first interface, index=1. (2) IF-MIB::ifIndex.2 = INTEGER: 2 — second interface, index=2. (3) IF-MIB::ifDescr.1 = STRING: \"GigabitEthernet0/0\" — textual name of first interface. (4) IF-MIB::ifDescr.2 = STRING: \"GigabitEthernet0/1\". (5) IF-MIB::ifType.1 = INTEGER: ethernetCsmacd(6) — IEEE 802.3 Ethernet. (6) IF-MIB::ifType.2 = INTEGER: ethernetCsmacd(6). (7) IF-MIB::ifSpeed.1 = Gauge32: 1000000000 — 1 Gbps in bits/s. (8) IF-MIB::ifSpeed.2 = Gauge32: 1000000000. (9) IF-MIB::ifOperStatus.1 = INTEGER: up(1) — interface is operationally up. (10) IF-MIB::ifOperStatus.2 = INTEGER: down(2) — interface is operationally down. OID notation explanation: IF-MIB::ifOperStatus.1 decodes as: IF-MIB = MIB module name; ifOperStatus = object name within the module; .1 = ifIndex value (instance identifier for row 1 of the ifTable). The MIB compiler maps the human-readable name to numeric OID 1.3.6.1.2.1.2.2.1.8.1."
  },
  "u1t8": {
    level1: "Five SNMP PDU types — detailed table: (1) GET PDU — sent by: Manager. Carries: a list of OIDs whose values are requested. Use: retrieve a known, specific OID value (e.g., sysUpTime.0). Sequence diagram — Manager → [GET: request-id=42, OIDs=[1.3.6.1.2.1.1.3.0]] → Agent. Agent → [RESPONSE: request-id=42, values=[Timeticks: 284562]] → Manager. (2) GETNEXT PDU — sent by: Manager. Carries: a list of OIDs; agent returns the lexicographically next OID after each specified one. Use: MIB tree traversal without knowing exact OIDs. Risk: will walk past end of intended subtree if Manager doesn't check returned OID prefix. (3) GETBULK PDU — sent by: Manager (SNMPv2c/v3 only). Carries: non-repeaters count, max-repetitions count, and list of OIDs. Agent returns non-repeaters OIDs once (GETNEXT-like) and max-repetitions successors for each repeater OID. Use: efficient table retrieval. (4) SET PDU — sent by: Manager. Carries: OID-value pairs to write to the Agent's MIB. Use: configuration changes (e.g., change sysName, enable/disable an interface). Requires read-write community string (v1/v2c) or authPriv credentials (v3). High risk: misconfigured SET on production device can cause outage. (5) RESPONSE PDU (GETRESPONSE) — sent by: Agent (in reply to GET/GETNEXT/GETBULK/SET from Manager). Carries: request-id (echoed from request for correlation), variable bindings (OID-value pairs), and error-status/error-index if any OID could not be retrieved. The Manager uses request-id to match responses to outstanding requests, which is essential because SNMP uses UDP (no connection-level ordering).",
    level2: "Step-by-step GETBULK trace for 12-row ifDescr table (non-repeaters=0, max-repetitions=5): Starting OID for ifDescr column: 1.3.6.1.2.1.2.2.1.2 (ifDescr object in ifTable). PDU Exchange 1: Manager sends GETBULK [OID: 1.3.6.1.2.1.2.2.1.2, max-rep=5]. Agent returns 5 values: ifDescr.1='Gi0/0', ifDescr.2='Gi0/1', ifDescr.3='Gi0/2', ifDescr.4='Gi0/3', ifDescr.5='Gi0/4'. Manager notes last returned OID = 1.3.6.1.2.1.2.2.1.2.5. Still within ifDescr subtree (prefix matches) → continue. PDU Exchange 2: Manager sends GETBULK [OID: 1.3.6.1.2.1.2.2.1.2.5, max-rep=5]. Agent returns 5 values: ifDescr.6='Gi0/5', ifDescr.7='Gi0/6', ifDescr.8='Gi0/7', ifDescr.9='Gi0/8', ifDescr.10='Gi0/9'. Manager notes last returned OID = 1.3.6.1.2.1.2.2.1.2.10. Still within prefix → continue. PDU Exchange 3: Manager sends GETBULK [OID: 1.3.6.1.2.1.2.2.1.2.10, max-rep=5]. Agent returns 5 values: ifDescr.11='Gi0/10', ifDescr.12='Gi0/11', ifType.1=6, ifType.2=6, ifType.3=6. BOUNDARY CONDITION: the last 3 values (ifType.1-3) have OID prefix 1.3.6.1.2.1.2.2.1.3 which is OUTSIDE the ifDescr subtree (1.3.6.1.2.1.2.2.1.2). The Manager discards these 3 out-of-subtree OIDs and stops. Total PDUs = 3. All 12 rows retrieved. Theoretical PDUs = ⌈12/5⌉ = 3 — matches actual.",
    level3: "RTT_ratio calculation for GETBULK vs GETNEXT: Formula: RTT_ratio = N_rows / ⌈N_rows / R⌉ = R (when N is a multiple of R). More precisely: RTT_ratio = N_getnext_pdus / N_getbulk_pdus = N / ⌈N/R⌉. For N=80 rows, R=10: GETNEXT PDUs = 80 (one per row). GETBULK PDUs = ⌈80/10⌉ = 8. RTT_ratio = 80/8 = 10. For N=80 rows, R=20: GETBULK PDUs = ⌈80/20⌉ = 4. RTT_ratio = 80/4 = 20. Percentage improvement when doubling max-repetitions from 10 to 20: RTT_ratio goes from 10 to 20 — a 100% improvement (doubled). In absolute terms: PDU count reduced from 8 to 4. Doubling max-repetitions exactly halves the PDU count (for tables that are evenly divisible). This linear relationship holds until R ≥ N, after which there is no further gain. For NMS deployments, this calculation justifies increasing max-repetitions from 10 to 20 for most standard interface tables — a zero-cost configuration change that halves polling time and CPU load on both manager and agent.",
    level4: "snmpbulkwalk vs GETNEXT-equivalent (max-rep=1) comparison: Run 1 — snmpbulkwalk with max-repetitions=10: Command: snmpbulkwalk -v2c -c public -Cn0 -Cr10 192.168.1.1 1.3.6.1.2.1.2.2. Total PDUs exchanged (from Wireshark): ⌈50/10⌉ = 5 GETBULK PDUs + 5 RESPONSE PDUs = 10 total. Elapsed time: ~50 ms (assuming 5 ms RTT × 5 round trips). Run 2 — GETNEXT-equivalent (max-rep=1): Command: snmpbulkwalk -v2c -c public -Cn0 -Cr1 192.168.1.1 1.3.6.1.2.1.2.2. Total PDUs exchanged: 50 GETBULK(R=1) PDUs + 50 RESPONSE PDUs = 100 total. Elapsed time: ~250 ms (50 round trips × 5 ms). Measured RTT_ratio: 250/50 = 5× improvement (matching max-repetitions=10 theoretical ratio). To measure in Wireshark: Filter 'udp.port == 161'. Count the number of SNMP GET packets per run. Observe that GETBULK R=1 response contains exactly 1 varbind per PDU (same as GETNEXT), while GETBULK R=10 response contains up to 10 varbinds per PDU. The theoretical ratio R=10 (ratio=10) is closely matched by the measured ratio (≈5–10, varying with RTT jitter). Any deviation from theoretical is due to: the final partial PDU (boundary condition), network RTT variance, and agent processing time."
  },
  "u1t9": {
    level1: "Complete CLI syntax for four SNMP commands: (1) snmpget — retrieves the exact value of one specified OID. Syntax: snmpget -v2c -c public 10.0.0.1 1.3.6.1.2.1.1.3.0. Flag meanings: -v2c = SNMP version 2c; -c public = community string 'public' (read-only); 10.0.0.1 = target agent IP; 1.3.6.1.2.1.1.3.0 = OID for sysUpTime instance. Expected output: DISMAN-EVENT-MIB::sysUpTimeInstance = Timeticks: (284562) 0:47:25.62. (2) snmpset — writes a new value to a specified OID. Syntax: snmpset -v2c -c private 10.0.0.1 1.3.6.1.2.1.1.5.0 s \"NewRouter01\". Flag meanings: -c private = read-write community string; 1.3.6.1.2.1.1.5.0 = sysName OID; s = data type string; \"NewRouter01\" = new value. Note: requires read-write community. Handle with extreme caution on production devices. (3) snmpwalk — traverses a MIB subtree using successive GETNEXT operations. Syntax: snmpwalk -v2c -c public 10.0.0.1 1.3.6.1.2.1.2.2. Flag meanings: 1.3.6.1.2.1.2.2 = top of ifTable (walks all interface rows and columns). Output: IF-MIB::ifIndex.1 = INTEGER: 1; IF-MIB::ifDescr.1 = STRING: 'GigabitEthernet0/0'; etc. (4) snmpbulkwalk — retrieves multiple rows per PDU using GETBULK. Syntax: snmpbulkwalk -v2c -c public -Cn0 -Cr20 10.0.0.1 1.3.6.1.2.1.2.2. Flag meanings: -Cn0 = non-repeaters=0 (no scalar prefix); -Cr20 = max-repetitions=20 (fetch 20 rows per PDU). This retrieves the same ifTable as snmpwalk but in ⌈rows/20⌉ PDU round trips instead of one-per-row.",
    level2: "snmpwalk traversal of system group (1.3.6.1.2.1.1): The walk uses successive GETNEXT operations starting from 1.3.6.1.2.1.1. At each step, the returned OID becomes the next request's OID. Five OID-name-value triples in lexicographic order: (1) SNMP-GET-NEXT(1.3.6.1.2.1.1) → SNMPv2-MIB::sysDescr.0 (1.3.6.1.2.1.1.1.0) = STRING: 'Cisco IOS Software, Version 15.4(3)M2, RELEASE SOFTWARE'. (2) GETNEXT(1.3.6.1.2.1.1.1.0) → SNMPv2-MIB::sysObjectID.0 (1.3.6.1.2.1.1.2.0) = OID: enterprises.9.1.1 (Cisco's enterprise OID). (3) GETNEXT(1.3.6.1.2.1.1.2.0) → DISMAN-EVENT-MIB::sysUpTimeInstance (1.3.6.1.2.1.1.3.0) = Timeticks: (284562) 0:47:25.62. (4) GETNEXT(1.3.6.1.2.1.1.3.0) → SNMPv2-MIB::sysContact.0 (1.3.6.1.2.1.1.4.0) = STRING: 'admin@university.edu'. (5) GETNEXT(1.3.6.1.2.1.1.4.0) → SNMPv2-MIB::sysName.0 (1.3.6.1.2.1.1.5.0) = STRING: 'CoreRouter-01'. Walk termination: the walk terminates when the returned OID falls outside the starting subtree prefix (1.3.6.1.2.1.1). After sysServices.0, the next OID in the MIB is 1.3.6.1.2.1.2.1 (ifNumber in the interfaces group) — which has a different prefix, so the walk stops.",
    level3: "T_walk and T_bulk calculation: Given: N = 200 MIB objects, R = 20 max-repetitions, RTT = 3 ms. T_walk = N × RTT = 200 × 3 = 600 ms (each GETNEXT fetches 1 object → 200 round trips). T_bulk = ⌈N/R⌉ × RTT = ⌈200/20⌉ × 3 = 10 × 3 = 30 ms. Speedup factor = T_walk / T_bulk = 600 / 30 = 20×. snmpbulkwalk (R=20) is 20 times faster than snmpwalk for 200 objects at 3 ms RTT. This directly equals the max-repetitions value R = 20 because 200 divides evenly by 20. In practical terms: polling a device with snmpwalk takes 600 ms; with snmpbulkwalk it takes 30 ms — for a 1000-device NMS polling every 5 minutes, this difference means snmpwalk uses 600,000 ms total per cycle (far exceeding the 300-second window) while snmpbulkwalk uses 30,000 ms (100 seconds — comfortably within window).",
    level4: "Lab measurement — snmpwalk vs snmpbulkwalk comparison: Step 1 — Run snmpwalk and start Wireshark simultaneously: Command: time snmpwalk -v2c -c public 192.168.1.1 1.3.6.1.2.1.2.2 > /dev/null. Wireshark filter: udp.port==161. Record: (a) total elapsed time displayed by `time` command, (b) count PDU pairs (GET + RESPONSE) in Wireshark. Step 2 — Run snmpbulkwalk: Command: time snmpbulkwalk -v2c -c public -Cn0 -Cr20 192.168.1.1 1.3.6.1.2.1.2.2 > /dev/null. Record same metrics. Example results for a 48-port switch (576 OID values — 48 rows × 12 ifTable columns): snmpwalk: 576 GETNEXT PDUs, ~2.88 seconds at 5ms RTT. snmpbulkwalk R=20: ⌈576/20⌉ = 29 GETBULK PDUs, ~145 ms. Measured speed ratio: 2880/145 ≈ 19.9×. Theoretical ratio: 576/29 = 19.9× — excellent agreement. Any discrepancy is due to network RTT jitter, agent response time variation, and the final partial PDU boundary. Conclusion: snmpbulkwalk with R=20 consistently achieves a speedup equal to the max-repetitions value R for tables where N >> R."
  },
  "u1t10": {
    level1: "Six SNMPv1 generic trap types and their triggers: (1) coldStart (generic trap type 0): Triggered when the SNMP agent initialises for the first time after a power cycle or hard reset. The device has reloaded its startup configuration. NOC response: verify configuration integrity, check for any unexpected parameter changes. (2) warmStart (generic trap type 1): Triggered when the SNMP agent re-initialises without power loss — typically a software process restart or SNMP agent daemon restart. The configuration is preserved. NOC response: investigate why the agent restarted (possible software bug or deliberate restart after config change). (3) linkDown (generic trap type 2): Triggered when a network interface transitions from up to down state. Carries varbind ifIndex to identify which interface failed. NOC response: check physical connectivity (SFP, cable), check remote device health, examine interface error counters. (4) linkUp (generic trap type 3): Triggered when an interface transitions from down to up. Indicates recovery. NOC response: confirm service restoration, check if the recovery was expected (maintenance window) or spontaneous (possible flapping link). (5) authenticationFailure (generic trap type 4): Triggered when the SNMP agent receives a PDU with an incorrect community string or authentication credentials. Indicates a misconfigured NMS tool or an unauthorised scan. NOC response: Security Management — investigate the source IP, review access logs, update community strings or SNMPv3 credentials if compromised. (6) egpNeighborLoss (generic trap type 5): Triggered when an EGP (Exterior Gateway Protocol) neighbour is lost. Rare in modern networks (EGP is obsolete, replaced by BGP). Modern equivalent: vendors define enterprise-specific traps for BGP peer loss.",
    level2: "Message sequence diagram — TRAP vs INFORM: TRAP delivery (unreliable, one-way): Agent → [TRAP PDU, UDP, fire-and-forget] → NMS (port 162). No ACK expected. No retransmission. If the UDP packet is lost, the NMS never knows the event occurred. INFORM delivery (reliable, acknowledged): Step 1: Agent → [INFORM PDU, UDP] → NMS (port 162). Agent starts retransmission timer (default: 1 second). Step 2: NMS receives INFORM, processes event, sends back: NMS → [RESPONSE PDU (GETRESPONSE)] → Agent. Step 3 (success): Agent receives RESPONSE → cancels retransmission timer → INFORM delivery complete. Step 3 (timeout, retry): If Agent timer expires with no RESPONSE: Agent → [INFORM PDU, retry #1] → NMS. Repeat up to configured retry count (default: 3). Labels in diagram: Sender = Agent (network device). Receiver = NMS. PDU type = INFORM (for reliable) vs TRAP (for unreliable). ACK = RESPONSE PDU sent by NMS only for INFORM. Timeout window = 1–5 seconds (configurable). Retry = 0–5 retransmissions (configurable). INFORM delivery fails only if ALL transmission attempts (original + all retries) are lost simultaneously.",
    level3: "INFORM delivery probability calculations: Formula: P_delivery = 1 − p^(n+1) where p = packet loss probability, n = number of retries (so total attempts = n+1). Given p = 0.10 (10% loss). Single TRAP (0 retries, n=0): P = 1 − 0.10^1 = 0.90 = 90%. This means 1 in 10 critical alarms is silently lost. INFORM with n=2 retries (3 total attempts): P = 1 − 0.10^3 = 1 − 0.001 = 0.999 = 99.9%. INFORM with n=3 retries: P = 1 − 0.10^4 = 1 − 0.0001 = 0.9999 = 99.99%. INFORM with n=5 retries: P = 1 − 0.10^6 = 1 − 0.000001 = 0.999999 = 99.9999%. Reliability gain of 5 retries over single TRAP: Single TRAP: 90.0%. INFORM with n=5: 99.9999%. Absolute gain: 9.9999%. Relative gain: (99.9999 − 90) / 90 × 100 ≈ 11.1% improvement. More meaningfully — failure probability comparison: TRAP failure = 10%; INFORM(n=5) failure = 0.0001%. INFORM(n=5) is 100,000× more reliable than a single TRAP on a 10% lossy link. Production recommendation: use INFORMs with at least 3 retries for any alarm that triggers automated remediation or SLA billing.",
    level4: "GNS3/Packet Tracer SNMP trap lab: Step 1 — Configure trap receiver on the 'NMS' host: sudo snmptrapd -v2c -c public -Lo -f (run in foreground, log to stdout, accept v2c community 'public'). Step 2 — Configure router to send traps: Router(config)# snmp-server community public RO. Router(config)# snmp-server host 192.168.1.100 version 2c public. Router(config)# snmp-server enable traps. Step 3 — Trigger linkDown event: Router(config)# interface GigabitEthernet0/1. Router(config-if)# shutdown. Step 4 — Capture TRAP PDU in Wireshark (filter: udp.port==162): In the Wireshark decode, identify the PDU structure. The TRAP PDU varbind list must contain: Varbind 1 (mandatory): OID = 1.3.6.1.2.1.1.3.0 (sysUpTime.0) = Timeticks: 284562 → this timestamps the event at the moment of occurrence. Varbind 2 (mandatory): OID = 1.3.6.1.6.3.1.1.4.1.0 (snmpTrapOID.0) = OID: 1.3.6.1.6.3.1.1.5.3 → this is the linkDown notification OID. Additional varbinds (context): ifIndex.2 = INTEGER: 2 (the interface that went down), ifAdminStatus.2 = INTEGER: down(2), ifOperStatus.2 = INTEGER: down(2). On the snmptrapd output, you should see: '2024-07-15 10:30:00 192.168.1.1: Trap enterprises.cisco.iso.org... link down on interface GigabitEthernet0/1.' This exercise confirms: (a) TRAPs are sent without being requested; (b) mandatory varbinds are always present; (c) additional varbinds provide context for alarm correlation."
  },
  "u1t11": {
    level1: "5 SNMP MIB (SMI) limitations and corresponding YANG features: (1) SMI Limitation: Limited type system — SMI supports only 8 base types (INTEGER, OCTET STRING, OBJECT IDENTIFIER, Counter32, Gauge32, TimeTicks, Opaque, IpAddress). Cannot express decimal numbers, boolean values, or union types. YANG Feature that addresses it: YANG built-in types — 18+ types including boolean, decimal64, bits, union, leafref, identityref, enumeration, inet:ip-address (from ietf-inet-types). (2) SMI Limitation: No constraint language — SMI offers only INTEGER range and OCTET STRING length constraints. Cannot express 'field A is required only when field B = X' or 'value must match a regex pattern'. YANG Feature: must and when XPath expressions — enforce arbitrary constraints and conditional presence rules validated before any configuration is applied. (3) SMI Limitation: No transaction semantics — SNMP SET operations are individual, non-atomic changes. Partial failure leaves device in inconsistent state. YANG Feature: NETCONF candidate datastore + commit — all YANG-defined changes are staged, validated against the YANG schema, then committed atomically (all-or-nothing). (4) SMI Limitation: Read-biased design — MIBs were designed for monitoring, not configuration. Configuration via SNMP SET is unreliable and lacks validation. YANG Feature: Dual config/state separation — YANG distinguishes config true (writable) from config false (read-only operational state), enabling safe, validated configuration management via NETCONF edit-config. (5) SMI Limitation: No reusability — MIB definitions cannot be composed or reused across modules. Groupings of related objects must be repeated in every MIB that needs them. YANG Feature: grouping and uses — define reusable schema fragments once (e.g., a 'common-interface-attributes' grouping), then use them in multiple containers and lists. Also: augment allows extending another module's schema without forking it.",
    level2: "Minimal YANG module stub for 'interface' container (10–15 lines): module my-interface { namespace \"urn:example:my-interface\"; prefix mi; description \"Minimal interface data model\"; revision 2024-07-15 { description \"Initial version\"; } container interface { description \"A single network interface\"; leaf name { type string; description \"Interface name (e.g. eth0, GigabitEthernet0/0)\"; } leaf enabled { type boolean; default true; description \"Administrative status: true=up, false=down\"; } leaf mtu { type uint16 { range \"68..65535\"; } units bytes; description \"Maximum Transmission Unit in bytes\"; } leaf description { type string; description \"Human-readable interface description (optional)\"; } } } Key constructs explained: module declares the top-level namespace unit; namespace provides a globally unique URI to avoid naming collisions; prefix is a short alias used when importing this module into another; container groups related leaves without a value itself; leaf defines a scalar data node with a specific type; type uint16 { range \"68..65535\" } adds a range constraint validated automatically by the NETCONF server; description is human-readable documentation embedded in the model.",
    level3: "YANG expressiveness ratio calculation: Formula: E = (T_YANG + C_YANG) / (T_SMI + C_SMI). Given: T_YANG = 18, C_YANG = 10, T_SMI = 8, C_SMI = 2. E = (18 + 10) / (8 + 2) = 28 / 10 = 2.80. YANG is 2.8 times more expressive than SMI with these parameters. Now recalculate assuming YANG 1.1 added 3 more constraint constructs (C_YANG becomes 13): E = (18 + 13) / (8 + 2) = 31 / 10 = 3.10. Change in E: 3.10 − 2.80 = +0.30 (an 10.7% increase in expressiveness ratio). Practical meaning: each additional constraint mechanism (e.g., YANG 1.1's action statement, anydata type, improved when/must XPath support) represents a type of data integrity rule that the NETCONF server enforces automatically — eliminating an entire class of configuration error that previously required manual operator review. The increase from E=2.8 to E=3.1 means YANG 1.1 can catch 10.7% more invalid configurations before they reach the network.",
    level4: "YANG evolution timeline — key milestones: 1990 — SMI/MIBs (RFC 1155 + RFC 1213): Problem solved — defined a standard hierarchical data schema for SNMP-managed objects, replacing proprietary per-vendor monitoring interfaces. Standards body: IETF. Limitation: 8 types, no constraints, read-biased, no transactions. 2002 — SPPI (RFC 3159): Problem solved — extended SMI for policy-based management (COPS-PR protocol). Better constraint support. Limited adoption outside policy frameworks. Standards body: IETF. Limitation: only applicable to COPS-PR, not general network management. 2006 — NETCONF Protocol v1.0 (RFC 4741): Problem solved — RFC 3535 (2003 operator requirements workshop) identified that operators needed transactional configuration with commit/rollback, candidate datastore, and configuration locking. NETCONF provided the transport/protocol framework. Standards body: IETF NETCONF working group. Limitation at this stage: used XML but had no standard data modelling language (operators wrote ad-hoc XML schemas). 2010 — YANG 1.0 (RFC 6020): Problem solved — provided a standard data modelling language for NETCONF. Replaced SMI for configuration data modelling. Introduced containers, lists, leafs, groupings, augments, and the must/when constraint language. Standards body: IETF NETMOD working group. Key milestone: YANG is the direct successor to SMI, designed to work natively with NETCONF. 2016 — YANG 1.1 (RFC 7950): Problem solved — added: action statement (define operations on data nodes), anydata (unstructured data), improved XPath in must/when expressions, better notification support, and clearer import/include semantics. Standards body: IETF NETMOD working group. Current standard: all new IETF, OpenConfig, and 3GPP YANG models use YANG 1.1."
  },
  "u1t12": {
    level1: "5 SNMP technical limitations paired with operator requirements violated (table format): Limitation 1: No transaction support (non-atomic SETs). Technical detail: Each SNMP SET PDU is an independent operation processed immediately. No way to group 10 SET operations into one atomic transaction. Operator requirement violated: Safe automated configuration management — operators need 'all-or-nothing' changes for multi-parameter configurations (BGP, OSPF, ACLs). Partial failure leaves device in inconsistent state causing outages. Limitation 2: Cleartext community strings (SNMPv1/v2c). Technical detail: Community strings are included in every PDU header in plaintext ASCII — trivially captured by any sniffer on the management network. Default strings 'public'/'private' widely known. Operator requirement violated: Secure management access — operators need cryptographic authentication and confidentiality for management credentials, especially required by PCI-DSS, ISO 27001, and FedRAMP. Limitation 3: Limited SMI data types (8 types, 2 constraint mechanisms). Technical detail: SMI cannot express decimal numbers, boolean, union types, or conditional constraints. Cannot validate 'VLAN ID must be 1–4094' or 'MTU must be set before IP address'. Operator requirement violated: Self-validating configuration models — operators need device-enforced validation that prevents invalid configurations from being applied, reducing human error rate. Limitation 4: Pull-only polling model (no streaming). Technical detail: SNMP requires the NMS to initiate every data request. Agents cannot push streaming telemetry to subscribers. Polling introduces detection latency equal to the polling interval. Operator requirement violated: Sub-second streaming telemetry for 5G networks — operators need per-second or per-100ms counter updates for SON (Self-Organising Network) algorithms, anomaly detection, and real-time QoS enforcement. Limitation 5: Designed for monitoring, not configuration at scale. Technical detail: SNMP SET lacks locking (multiple managers can conflict), validation (no schema enforcement), bulk operations (no way to send 100 changes atomically), and rollback. Operator requirement violated: Zero-touch provisioning (ZTP) for 5G SA rollout — operators need to provision thousands of gNBs automatically with complex multi-parameter configurations, which SNMP SET cannot safely deliver.",
    level2: "Partial configuration problem explained with BGP example: Scenario: An operator needs to configure a new BGP peer on a router. This requires 8 sequential SNMP SET operations: (1) SET bgpPeerAdminStatus = stop, (2) SET bgpPeerRemoteAs = 65002, (3) SET bgpPeerLocalAddr = 10.0.0.1, (4) SET bgpPeerRemoteAddr = 10.0.0.2, (5) SET bgpPeerHoldTimeConfigured = 90, (6) SET bgpPeerKeepAlive = 30, (7) SET routePolicy = IMPORT_FILTER_01, (8) SET bgpPeerAdminStatus = start. If operation 5 (holdtime SET) fails due to a timeout: Operations 1–4 have already been applied to the running configuration. Operations 5–8 are NOT applied. The device now has a partially configured BGP peer: remote AS and addresses are set, but timers are wrong and admin status is still 'stop'. The BGP session never comes up, but the device shows the peer as configured. Diagnosing this inconsistency requires manual per-OID inspection. NETCONF solution with candidate datastore: The NMS locks the candidate datastore, sends all 8 changes as a single edit-config XML message, the NETCONF server validates all 8 changes against the YANG BGP model (checking types, ranges, mandatory fields) without applying any of them. Then the NMS sends commit. The server atomically applies all 8 changes to the running datastore simultaneously. If any single operation fails during commit, the server automatically reverts to the pre-change state (confirmed-commit with rollback). Either all 8 BGP parameters are applied, or none are — no partial state is possible.",
    level3: "P_fail calculation for multi-step SNMP configuration: Formula: P_fail = 1 − (1 − p_set)^k where p_set = per-step failure probability, k = number of steps. For k=5 steps, p_set=0.03: P_fail = 1 − (1 − 0.03)^5 = 1 − 0.97^5 = 1 − 0.8587 = 0.1413 = 14.13%. For k=15 steps, p_set=0.03: P_fail = 1 − 0.97^15 = 1 − 0.6333 = 0.3667 = 36.67%. Ratio of risks: P_fail(k=15) / P_fail(k=5) = 36.67% / 14.13% = 2.59×. Interpretation: A 15-step configuration change is 2.59× more likely to cause a partial failure than a 5-step change, at the same per-step failure rate. This means that as configuration complexity grows (more steps), the risk of a partial failure grows faster than linearly — because each additional step is an additional opportunity for failure while all previous steps remain applied. The practical implication: SNMP SET is relatively safe for 1–2 step changes (P_fail = 3–6%), but becomes dangerous for anything requiring 5+ steps (P_fail > 14%). BGP configurations routinely require 8–15 steps; large-scale device provisioning may require hundreds — making SNMP SET fundamentally unsafe for automated production provisioning.",
    level4: "RFC 3535 Section 2 — Top 5 Operator Requirements and their resolutions: Requirement 1: 'Ease of use is more important than power.' Operators wanted management tools that worked like familiar IT tools — not complex protocol stacks. Resolution: YANG addressed this by enabling human-readable data models; NETCONF/YANG tooling (pyang, ncclient) is comparable in usability to web API tools. RESTCONF (RFC 8040) further addressed this with HTTP-based YANG access using familiar REST patterns. Requirement 2: 'Operators want to be able to make configuration changes from scripts and tools, not just manual GUIs.' They needed transactional, scriptable configuration. Resolution: NETCONF fully addressed this — ncclient Python library, Ansible netconf_config module, and Terraform NETCONF providers all enable scriptable configuration with commit/rollback. SNMP SET (non-transactional) directly violated this requirement. Requirement 3: 'Operators want to be able to retrieve the difference between the configured state and the current operational state.' They needed running-vs-startup config diff capability. Resolution: NETCONF datastores (running, candidate, startup) directly address this — operators can compare running and startup datastores using get-config on both and diff the XML. Requirement 4: 'Operators want a consistent interface to manage multiple devices from different vendors.' Multi-vendor consistency was critical. Resolution: Standard YANG modules (IETF ietf-interfaces, ietf-routing, OpenConfig models) address this — a single YANG-based NMS can manage Cisco, Juniper, and Nokia devices using the same data model. SNMP enterprise MIBs were fundamentally inconsistent across vendors. Requirement 5: 'Operators want roll-back of configuration changes (undo).' This was the most critical missing SNMP capability. Resolution: NETCONF confirmed-commit (RFC 6241 section 8.4) directly implements this — a commit can be confirmed with a timeout; if not re-confirmed within the timeout, the device automatically reverts. Remains unaddressed by SNMPv3. Fully addressed by NETCONF."
  },
  "u2t1": {
    level1: "Install pyang (`pip install pyang`). Download the `ietf-interfaces.yang` module (RFC 7223) from GitHub. Run `pyang -f tree ietf-interfaces.yang` and capture the output. Identify 3 containers, 2 lists, and 5 leaf nodes in the tree output.",
    level2: "Write a 10-line YANG module defining a `host` data structure with: name (string), ip-address (string), os-type (enumeration), cpu-load (uint8 0-100). Validate it with `pyang host.yang`. Fix any errors pyang reports. Explain each error and how you resolved it.",
    level3: "Read the YANG 1.1 specification (RFC 7950) section on 'must' expressions. Write a YANG module for a `vlan-pool` container that contains a list of VLAN ranges and uses a `must` constraint to ensure ranges do not overlap and are within 1-4094. Validate with pyang. Write XPath test cases for non-overlapping and overlapping ranges.",
    level4: "Set up a NETCONF sandbox using Docker (sysrepo/netopeer2 image). Write a YANG module for a simple 'acl' data model (list of rules with match src-ip, dst-ip, action permit/deny). Compile and load the module into sysrepo. Use `netopeer2-cli` to query the ACL data tree via NETCONF <get>. Then write a Python script using ncclient to add an ACL rule via <edit-config>."
  },
  "u2t2": {
    level1: "Open the YANG RFC 7950 and locate the definitions of: leaf, leaf-list, container, list, choice, case, grouping, uses, typedef, and identity. For each, copy the one-sentence definition from the RFC and write your own example.",
    level2: "Write a YANG module defining a `network-device` (container) with: hostname, interfaces (list of name, type, mtu, enabled), routing-table (list of prefix, next-hop), and ntp-servers (leaf-list). Use proper types (string, uint32, boolean, inet:ip-address). Validate with pyang.",
    level3: "Design a YANG module for a `qos-policy` with: a policy container that uses a `choice` between `rate-limit` (bandwidth bps) and `priority-queue` (strict|weighted), a `grouping` for common match-criteria (src-ip, dst-ip, dscp), and a `typedef` for bandwidth-with-unit. Write XPath `when` expressions to show/hide fields based on the queue type selected.",
    level4: "Build a YANG module dependency analyser in Python: (a) scan a directory of YANG modules, (b) parse `import` and `include` statements from each module, (c) build a directed graph of module dependencies using NetworkX, (d) detect and report circular dependencies, (e) compute the topological compilation order, (f) visualise the graph with module revision numbers as node labels."
  },
  "u2t3": {
    level1: "Read RFC 6020 section 9.9 on XPath usage in YANG. List 5 XPath functions that YANG supports (e.g., `current()`, `deref()`, `enum-value()`). For each, write a one-line example of when you would use it in a `must` or `when` expression.",
    level2: "Write a YANG module for a `firewall-rules` container with a list of rules. Add a `must` expression that prevents overlapping CIDR ranges: `derived-from-or-self(./src-ip, 'inet:ipv4-prefix')` — implement the full constraint using `current()` and XPath comparison operators. Validate with pyang.",
    level3: "Given a YANG data tree with: `/interfaces/interface[name='eth0']/ipv4/address` list of 3 entries, write XPath expressions for: (a) select the address with prefix-length > 24, (b) check if any interface has no IPv4 address assigned, (c) count how many interfaces are enabled, (d) find all addresses in the 10.0.0.0/8 subnet. Use `pyang` to validate expressions against the `ietf-interfaces` module.",
    level4: "Implement an XPath evaluator for YANG data trees in Python: (a) parse a YANG module into an in-memory data tree (using pyangbind or yangson), (b) accept user-input XPath expressions, (c) evaluate them against the data tree, (d) return the matching nodes with their values, (e) support `must` and `when` expression evaluation for constraint checking. Test with 20 predefined XPath queries covering 5 different YANG modules."
  },
  "u2t4": {
    level1: "Read RFC 7950 sections on RPC and notification statements. Draw a sequence diagram showing: client → NETCONF server: <rpc><my-rpc><param1>value</param1></my-rpc></rpc> → server processes → server → client: <rpc-reply><status>ok</status></rpc-reply>. Label the message-id and the RPC name.",
    level2: "Write a YANG module that defines: (a) an RPC `reset-port` with input parameters `port-name` (string) and `confirm` (boolean), and output `result` (enum success/failure/rejected), (b) a notification `port-alert` with fields `port-name`, `severity` (critical/major/minor), and `alert-message`. Validate with pyang.",
    level3: "Create two conflicting YANG modules: Module A defines a leaf `max-sessions` as type uint32 with default 1000. Module B augments Module A by adding a `must` constraint 'max-sessions <= 500'. Demonstrate via pyang that both modules can be imported simultaneously but the constraint is only enforced when Module B is loaded. Write a Python script that detects such augment conflicts across a module dependency tree.",
    level4: "Build a YANG RPC server using Python and sysrepo: (a) write a YANG module with 3 RPCs — `get-device-stats` (returns CPU/mem/disk), `reboot-device` (requires confirm flag), `schedule-maintenance` (accepts start-time and duration), (b) load the module into sysrepo, (c) implement RPC handlers in Python that return realistic simulated data, (d) write a NETCONF client using ncclient that calls each RPC and displays the response."
  },
  "u2t5": {
    level1: "Draw a sequence diagram showing the NETCONF transport layer: SSH TCP handshake → SSH key exchange → NETCONF <hello> exchange with capabilities → application messages (<get>, <edit-config>) → <close-session>. Label the TCP ports, SSH channels, and XML message boundaries.",
    level2: "Using Docker, start a sysrepo/netopeer2 container. Connect to it using SSH on port 830. Capture the `<hello>` XML message — identify the capabilities URI (urn:ietf:params:netconf:capability:candidate:1.0 etc.). Then send a `<get-config>` with source=running and filter the interfaces subtree. Record the response XML.",
    level3: "Write a Python script using ncclient to: (a) connect to a NETCONF server with SSH key authentication, (b) retrieve the server's capabilities and print them, (c) send `<get>` with no filter and count the total number of XML nodes in the response, (d) send `<edit-config>` on candidate with a merge operation to add a new loopback interface, (e) commit and validate the change. Measure and report the time for each operation.",
    level4: "Build a NETCONF configuration auditor: (a) Python script that connects to 3 virtual devices, (b) retrieves the running configuration from each via `<get-config>`, (c) persists configs in a Git repository with device name and timestamp, (d) runs a diff between consecutive snapshots per device, (e) classifies changes as 'planned' (allowed by policy) or 'drift', (f) sends an email alert when drift is detected. Test by introducing an unplanned config change on one device."
  },
  "u2t6": {
    level1: "Create a table listing all standard NETCONF datastores (running, candidate, startup, operational, intended) with: RFC reference, which operations are valid on it, whether it is writable, and whether it persists across reboots.",
    level2: "Using the netopeer2 container, practice: (a) <lock> running and confirm the lock via netopeer2-cli, (b) <edit-config> on candidate to change a DNS server entry, (c) <validate> candidate, (d) <commit> with confirmed-commit timeout=120, (e) <discard-changes> before timeout to roll back. Capture each XML operation and response.",
    level3: "Calculate the storage overhead of maintaining 3 datastores (running, candidate, startup) for a device with 5000 configuration nodes averaging 200 bytes each. If each changed node is stored as a diff rather than full copy, what is the worst-case savings for a 50-node change? Write a Python script that models datastore storage for N nodes and M changes.",
    level4: "Implement a NETCONF datastore synchroniser: (a) Python script that periodically (every 5 minutes) compares running and startup datastores on a device via `<get-config>`, (b) computes a structural diff (added/removed/modified nodes), (c) if running ≠ startup, prompts the user to `<copy-config>` from running to startup, (d) logs all synchronisation events to a SQLite database, (e) provides a CLI to query sync history per device per date range."
  },
  "u2t7": {
    level1: "Open the ietf-interfaces YANG tree using `pyang -f tree ietf-interfaces.yang`. Identify: which nodes are configuration (config true) vs operational state (config false), which nodes contain keyed lists, and which nodes use leafrefs. Count the total number of leaf-level data nodes.",
    level2: "Using ncclient, retrieve the full `ietf-interfaces` subtree from a NETCONF device using a subtree filter: `<filter type=\\\"subtree\\\"><interfaces xmlns=\\\"urn:ietf:params:xml:ns:yang:ietf-interfaces\\\"/></filter>`. Parse the XML response and extract: number of interfaces, each interface's name/type/oper-status, and any IPv4 addresses configured.",
    level3: "Design an XPath 1.0 filter expression that retrieves only interfaces that are: (a) enabled and have oper-status 'up', (b) have IPv4 addresses in the 10.0.0.0/8 range, (c) are type 'ethernetCsmacd' and have MTU > 1500. Implement each as a NETCONG `<filter>` using the `xpath` attribute and test against a device.",
    level4: "Build a NETCONF data extraction pipeline: (a) Python script that connects to 5 devices, (b) for each device, applies 4 different subtree filters (interfaces, routing, VLANs, ACLs) sequentially, (c) parses each XML response into a Pandas DataFrame, (d) merges all device DataFrames into a single topology DataFrame with device_name, interface, neighbor_ip, vlan columns, (e) exports to CSV and generates a graph showing inter-device connections using NetworkX."
  },
  "u2t8": {
    level1: "Install `curl` and send a GET request to a RESTCONF-enabled device at `http://device:8080/restconf/data/ietf-interfaces:interfaces`. Use HTTP Basic auth and `Accept: application/yang-data+json` header. Document the request URL, headers, and response JSON structure. Explain how it differs from NETCONF XML.",
    level2: "Using Postman (or `curl`), perform the following RESTCONF operations on a lab device: (a) GET all interfaces, (b) GET a specific interface by name, (c) PATCH to change an interface description, (d) POST to add a new IPv4 address to an interface, (e) DELETE to remove the added address. Save each request as a Postman collection.",
    level3: "Compare RESTCONF and NETCONF across 8 dimensions: transport protocol, data encoding, operation semantics, transaction support, filtering capability, notifications, security model, and tooling ecosystem. Create a decision matrix and write a recommendation for (a) a small campus network vs (b) a large service provider.",
    level4: "Build a RESTCONF API gateway using Python (FastAPI): (a) implement a proxy that accepts RESTCONF-like requests and forwards them to backend devices, (b) add response caching with ETag support — return 304 Not Modified when data hasn't changed, (c) implement rate limiting per device (max 10 req/s), (d) add a request transformation layer that converts PATCH to merge operations, (e) test with wrk or locust at 100 concurrent requests and measure latency improvement from caching."
  },
  "u2t9": {
    level1: "Open a RESTCONF browser (or use `curl` to query `http://device:8080/.well-known/host-meta`) and discover the RESTCONF root URI (`/restconf`). Then GET the `/restconf/data` resource to list all top-level YANG data stores available on the device. Count and categorise them.",
    level2: "Using curl, send a RESTCONF PATCH request to modify a specific interface's MTU: `PATCH /restconf/data/ietf-interfaces:interfaces/interface=GigabitEthernet0/0/0/ietf-interfaces:mtu` with JSON body `{\\\"ietf-interfaces:mtu\\\": 1500}`. Verify the change via GET. Then attempt to set MTU to 65536 (out of range) and capture the error response — document the error-tag, error-type, and error-message.",
    level3: "Write a Python script using the `requests` library that benchmarks RESTCONF performance: (a) send 100 GET requests each for (1) a single leaf, (2) a container with 10 children, (3) a list with 50 entries, (b) measure p50, p95, p99 latency for each type, (c) compute the overhead ratio: `(time_list / time_leaf) / entry_count`, (d) plot the results with error bars and explain the scalability characteristics.",
    level4: "Design and implement a stateful RESTCONF application: (a) a Python (FastAPI) service that periodically polls a RESTCONF device for interface statistics, (b) stores the streaming data in PostgreSQL with time-series extension, (c) exposes a REST API that returns historical interface utilisation with aggregations (avg/max/p95 over 1h/24h/7d), (d) implements a simple threshold rule engine — if utilisation exceeds 90%, POST a notification to a webhook URL, (e) provides a health dashboard using chart.js."
  },
  "u2t10": {
    level1: "Open Postman and set up an environment with variables: `base_url`, `username`, `password`, `interface_name`. Write a single GET request to `{{base_url}}/restconf/data/ietf-interfaces:interfaces/interface={{interface_name}}` and verify it works. Save this as a reusable template.",
    level2: "Create a Postman Collection with 5 RESTCONF requests that form a workflow: (1) GET all interfaces, (2) GET a specific interface, (3) PATCH description and MTU of the interface, (4) POST to add a new IPv4 address, (5) DELETE to remove the added address. Use Postman tests to assert response status codes (200, 204, 201) and verify JSON response bodies contain expected fields.",
    level3: "Write a Newman (Postman CLI) script that: (a) runs the above collection against a target device, (b) extracts response values using Postman test scripts (e.g., pm.response.json().interface[0].name), (c) exports results to a JSON file, (d) reads the JSON and generates an HTML report with pass/fail per request, response time, and payload size. Run the collection 3 times and report average metrics.",
    level4: "Build a Postman-based RESTCONF monitoring and testing framework: (a) design a Postman collection that tests all CRUD operations across 3 different YANG data models (interfaces, VLAN, ACL), (b) write pre-request scripts that generate unique test data (random interface descriptions, unique IPs), (c) write test scripts that validate response schema using `ajv` JSON validator, (d) use Newman CI integration to run the collection hourly, (e) store results in InfluxDB and visualise pass/fail trends over time in Grafana."
  },
  "u3t1": {
    level1: "Start a syslog server (e.g., Kiwi Syslog or rsyslog on Linux). Generate 10 different test alarm messages manually using `logger` command: 2 critical, 3 major, 3 minor, 2 warning. Verify each appears in the syslog viewer with correct severity.",
    level2: "Open a network topology in GNS3 with 5 devices (3 routers, 2 switches). Manually shut down an interface on router-1. Observe the cascade of syslog messages: interface-down → OSPF neighbour-down → route withdrawal → BGP session-down. Capture all generated alarms and draw a dependency tree showing the root cause and all secondary alarms.",
    level3: "Analyse a 24-hour alarm CSV file (provided by instructor) with 500 entries from a simulated ISP network. For each of the top 5 most frequent alarm types, compute: (a) hourly rate, (b) average duration, (c) correlation coefficient with other alarm types, (d) day/night ratio. Identify which alarm type is most likely to be a root cause vs symptomatic.",
    level4: "Build a real-time alarm ingestion and display system: (a) Python listener on UDP 514 that receives syslog messages, (b) parses severity and facility from each message, (c) classifies each message into FCAPS category using keyword matching, (d) stores in Elasticsearch, (e) Kibana dashboard showing alarm rate over time (time-series bar chart), top 10 alarm sources (table), severity distribution (pie chart), and latest 50 alarms (data table). Configure a threshold alert: if alarm rate exceeds 10/min for 2 min, send email."
  },
  "u3t2": {
    level1: "Create a printed or digital reference card showing all alarm severity levels defined by ITU-T X.733 and IETF: Critical, Major, Minor, Warning, Indeterminate, Cleared. For each severity, write: (a) definition, (b) typical response time target, (c) example alarm from a router, (d) what colour it would be on a NOC dashboard.",
    level2: "For a set of 10 real-world network incidents (described in a provided scenario sheet), classify each into: severity (critical/major/minor/warning), FCAPS category (fault/config/perf/account/security), and domain (IP/optical/transport). Justify each classification in one sentence.",
    level3: "Design a severity assignment matrix: given a device checklist (core router, access switch, firewall, WAN link, DNS server) and alarm types (down, high-latency, CRC-error, config-changed, cpu-high), assign initial severity for each combination. Then write a Python function that computes dynamic severity based on: (a) number of impacted subscribers, (b) duration, (c) time of day (business hours vs night). Test with 10 scenarios.",
    level4: "Build an alarm severity reclassification engine: (a) ingest alarms from a CSV file, (b) for each alarm, apply a rule set (YAML-configurable) that checks device role, time of day, maintenance window status, and current network health score, (c) reclassify severity up or down based on rules, (d) log original vs reclassified severity with reason, (e) compute the percentage of alarms reclassified and the average severity delta, (f) generate a before/after severity distribution chart."
  },
  "u3t3": {
    level1: "Draw a flowchart showing the alarm correlation process: raw alarm ingestion → normalisation → deduplication → temporal grouping → topological correlation → root-cause identification → incident creation. Label each step with the data structure it operates on.",
    level2: "Given a network topology of 8 devices (Core-A, Core-B → Dist-1/2 → Access-1/2/3/4) and a cable cut between Core-A and Dist-1, simulate the alarm cascade: Dist-1 loses uplink → Access-1/2 lose uplink → OSPF reconverges → BGP prefixes withdrawn. Create a correlation rule that groups these into a single incident and identifies Core-A ↔ Dist-1 link cut as root cause.",
    level3: "Write a Python script that implements window-based alarm correlation: (a) read alarms from a JSON file with timestamp, source, type, (b) group alarms occurring within a 5-minute sliding window, (c) within each group, apply topological rules: if source A depends on source B, and both have alarms, mark B as root cause, (d) output correlated incidents with root-cause tag, (e) evaluate on a test dataset of 200 alarms with known ground truth — compute precision and recall.",
    level4: "Implement a graph-based root cause analysis engine: (a) build a NetworkX dependency graph from a topology CSV (device, depends_on, service), (b) implement BFS from each alarm source to find common ancestors, (c) implement PageRank to rank probable root causes by centrality, (d) implement random-walk with restart to simulate fault propagation, (e) ensemble the 3 methods with weighted voting, (f) test on 5 pre-constructed fault scenarios and compare each method's accuracy against ground truth."
  },
  "u3t4": {
    level1: "List 5 different situations where alarm suppression is appropriate (e.g., planned maintenance window, known software bug triggers false alarm). For each, write: suppression condition, scope (device/network/service level), and potential risk of suppressing real faults.",
    level2: "Given a maintenance schedule for next week (device list, start/end times per device), write a Python script that: (a) reads the schedule from a CSV, (b) when processing an alarm, checks if the source device is in a maintenance window, (c) if yes, suppresses the alarm and logs to a suppression audit file, (d) returns 'suppressed' or 'forwarded' status. Test with 5 alarm scenarios during and outside maintenance windows.",
    level3: "Design a multi-layer suppression policy: Layer 1 (device) — suppress if device in maintenance, Layer 2 (network) — suppress if parent link is down (derivative alarm), Layer 3 (service) — suppress if service has active SLA breach (non-critical components). Implement in Python with YAML-configurable rules. Evaluate on a test set of 500 alarms: calculate suppression rate (%), false-positive suppression rate (suppressed but real fault), and alarm reduction ratio.",
    level4: "Build a suppression policy management dashboard: (a) Flask web app, (b) CRUD for suppression rules (condition, scope, time window, operator comment), (c) dry-run mode — process alarms against rules and show which WOULD be suppressed without actually suppressing, (d) audit log viewer with filter by date/device/rule, (e) effectiveness statistics — alarm reduction percentage over time, most active suppression rules, operator override frequency. Include a real-time suppression activity feed."
  },
  "u3t5": {
    level1: "Draw the alarm lifecycle state machine: Raised → Acknowledged → Investigating → Escalated → Resolved → Closed. For each transition, write the trigger event (e.g., operator clicks 'Acknowledge') and the expected time-to-complete SLA target (e.g., Critical: acknowledge within 5 min).",
    level2: "Using Jira or a similar ticketing system (or mock it in a spreadsheet), create and track one alarm through its lifecycle: (a) create ticket for a Critical alarm 'Core-Router-01 LinkDown', (b) set priority to Critical and assign to Tier-1 NOC, (c) add three investigation notes, (d) escalate to Tier-2 after 15 min, (e) add resolution note 'Replaced SFP module', (f) close ticket. Record the time at each step.",
    level3: "Write a Python class `AlarmLifecycle` that: (a) initialises with alarm_id, severity, source, (b) implements state transitions with guards — cannot transition from Raised→Resolved directly (must go through Acknowledged→Investigating), (c) logs each transition with timestamp and operator_id, (d) computes SLA: acknowledge time, resolve time for each severity level, (e) returns SLA breach (true/false) based on configured thresholds per severity.",
    level4: "Build a complete alarm lifecycle management system: (a) Python/Flask backend with PostgreSQL, (b) REST API to create/acknowledge/escalate/resolve/close alarms, (c) enforces state machine rules (no illegal transitions), (d) auto-escalation: if alarm persists in Investigating >30 min (Critical) or >60 min (Major), auto-escalate to Tier-2, (e) SLA breach detection and notification, (f) React dashboard showing active alarms, lifecycle timeline per alarm, current SLA compliance %, and escalation chain status."
  },
  "u3t6": {
    level1: "Collect a labelled dataset of 50 historical alarms from your instructor or generate synthetic data. For each alarm, note: device_type, alarm_type, time_of_day, day_of_week, historical_frequency, current_network_health. Prepare this as a CSV ready for ML training.",
    level2: "Train a Random Forest classifier using scikit-learn on the dataset. Use 80% for training, 20% for testing. Print the confusion matrix, classification report (precision, recall, f1-score), and feature importance ranking. Identify which 3 features most influence severity classification.",
    level3: "Evaluate 3 different ML models (Random Forest, XGBoost, and Logistic Regression) for alarm severity prediction. Compare: accuracy, precision per severity class, training time, inference time per alarm, and model size. Plot ROC curves for each model as multi-class OvR. Recommend the best model for real-time inference (<10ms per alarm) and justify.",
    level4: "Implement an online ML inference service for severity reclassification: (a) train an XGBoost model on historical alarm data and export as `.joblib`, (b) build a FastAPI inference endpoint `/predict` that accepts alarm JSON and returns predicted severity with confidence score, (c) integrate with a live alarm stream — for each incoming alarm, call the inference endpoint and reclassify if confidence > 0.85, (d) log discrepancies between predicted and operator-assigned severity, (e) weekly retraining script that incorporates operator feedback."
  },
  "u3t7": {
    level1: "Find the latitude and longitude coordinates for 10 real or simulated network sites (e.g., 'REVA University, Bangalore' → 13.1234°N, 77.5678°E). Create a CSV with site_name, lat, lon, device_count, and alarm_count.",
    level2: "Using Leaflet or Google Maps API, create a web page that plots the 10 sites as markers. Colour-code markers by alarm count: green (0-5), amber (6-20), red (>20). When clicking a marker, show a popup with site name, device count, and alarm count.",
    level3: "Write a Python script that converts a network topology (devices with lat/lon) and alarm data into a GeoJSON feature collection. Each feature should be a Point with properties: device_name, alarm_count, worst_severity, last_alarm_time. Use `folium` to render the GeoJSON on an interactive map with a heatmap overlay showing alarm density.",
    level4: "Build a real-time GIS alarm dashboard: (a) use WebSocket to stream alarms from a Python backend to a web UI, (b) render alarms on a Leaflet map with animated markers, (c) implement marker clustering at zoom levels < 10 (group nearby markers with alarm count), (d) add a temporal playback control — scrub through the last 24 hours of alarm history at 5x speed, (e) overlay fibre routes between sites as GeoJSON lines coloured by utilisation, (f) add a panel showing the most recent alarm at the clicked location."
  },
  "u3t8": {
    level1: "Open the Kibana 'Discover' page (or Elasticvue) and search for alarm logs in the last 7 days. Create filters for: severity=Critical, time range = last 24h, source IP = specific device. Save the search as a saved search. Export the results as CSV.",
    level2: "Given a time-series database (InfluxDB or TimescaleDB) with 30 days of alarm data, write SQL/Flux queries to: (a) count alarms per severity per day, (b) find the top 5 most alarm-prone devices, (c) calculate average time between consecutive alarms per device, (d) find periods where alarm rate exceeded 10 per hour. Export results as CSV and create a summary chart.",
    level3: "Analyse a 30-day alarm dataset with 10,000 entries to find the top 3 most common alarm patterns: (a) compute n-gram alarm sequences (pairs, triples), (b) identify the most frequently co-occurring alarm pairs and compute their Jaccard similarity coefficient, (c) find the most common alarm lead-lag patterns (e.g., 'Alarm_B follows Alarm_A within 5 min'), (d) visualise the top 10 alarm pairs as a chord diagram.",
    level4: "Build a time-series alarm forensics platform: (a) Streamlit or Dash web app, (b) date range picker and device filter dropdown, (c) multi-panel display: alarm frequency timeline (bar chart with hourly aggregation), alarm heatmap (day vs hour of week), device correlation matrix (which devices alarm together), and raw alarm log table with search/filter, (d) one-click correlation: select a time window around a critical alarm → automatically overlay SNMP performance metrics (CPU, memory, interface utilisation) from the same period, (e) export investigation report as PDF."
  },
  "u3t9": {
    level1: "Start a Kafka broker (using Docker: `docker run -p 9092:9092 apache/kafka`). Create a topic 'network-alarms'. Write a Python producer that sends 100 alarm messages to the topic at 1/sec. Write a consumer that reads and prints them. Verify message delivery with Kafka CLI consumer.",
    level2: "Create Python scripts for 3 domain-specific alarm producers: (a) 'ip-alarms' — generates SNMP-like interface down/up traps, (b) 'optical-alarms' — generates signal degrade/loss-of-light alarms, (c) 'power-alarms' — generates UPS/rectifier alarms. Each producer writes to a separate Kafka topic. Write a normalisation consumer that reads from all 3 topics, converts to a common schema (id, timestamp, domain, type, severity, source, description), and writes to a unified 'normalized-alarms' topic.",
    level3: "Design a canonical alarm data model for multi-domain unification. Map 3 different domain-specific severity scales to a unified 5-level scale. Implement in Python: (a) a parser for each domain's alarm format (JSON/XML/syslog), (b) a normalisation function per domain, (c) a test suite with 10 test cases per domain that validates correct mapping. Compute the normalisation accuracy (%).",
    level4: "Build a cross-domain alarm correlation pipeline: (a) ingest alarms from 3 Kafka topics (IP, optical, power), (b) normalise to canonical schema, (c) implement 5 correlation rules linking across domains, (d) e.g., 'optical signal-degrade → IP link-flap → BGP session-down', (e) output correlated incidents to Elasticsearch, (f) build a Grafana dashboard showing cross-domain incident rate, domain contribution pie chart, and per-domain alarm rate time-series, (g) alert when cross-domain incidents > 5 per hour."
  },
  "u3t10": {
    level1: "Define 5 alarm quality KPIs: Accuracy, Timeliness, Uniqueness, Actionability, Noise Score. For each KPI, write: (a) formula, (b) target value (e.g., Actionability > 60%), (c) how it is computed from raw alarm and incident data.",
    level2: "Given a dataset of 1000 alarms with operator labels ('action taken' vs 'ignored'), compute: (a) Accuracy — % of alarms confirmed by real incidents, (b) Uniqueness — ratio of unique alarms to total, (c) Actionability — % of alarms that led to operator action, (d) Noise Score = 1 - Actionability. Create a bar chart comparing these KPIs across 5 device types.",
    level3: "Write a Python script that computes 30-day rolling alarm quality KPIs per device, per alarm type, and per severity: (a) reads alarm log and incident log from CSV, (b) joins alarms to incidents on alarm_id and source, (c) computes daily KPIs with 30-day moving average, (d) detects devices with degrading quality (KPI trend slope < -0.1 over 7 days), (e) generates recommendations: 'Device X has 90% duplicate rate — increase debounce to 30s'. Output a prioritised action list.",
    level4: "Build a continuous alarm quality improvement dashboard: (a) Flask backend computing daily KPIs from alarm database, (b) store KPI history in PostgreSQL, (c) React frontend with panels for: current KPIs (big-number gauges), trend sparklines per KPI over 90 days, device ranking by noise score, automated recommendations list with 'dismiss'/'implement' buttons, (d) track implemented recommendations and show KPI improvement over time, (e) export monthly quality report as PDF."
  },
  "u4t1": {
    level1: "Install Mininet (`pip install mininet` or use the Mininet VM). Run the default minimal topology: `sudo mn --test pingpair`. Observe the OpenFlow switch being created. Then run `sudo mn --topo linear,4 --controller remote` to connect to a remote controller. Capture the output showing 4 switches being created.",
    level2: "Start a Ryu controller (`ryu-manager ryu/ryu/app/simple_switch_13.py`) and Mininet with `--controller remote`. From mininet CLI, run `pingall` to verify connectivity. On the controller terminal, observe the PACKET_IN messages. Count how many PACKET_INs are needed before all flows are installed. Use Wireshark to capture the OpenFlow 1.3 PACKET_IN and FLOW_MOD messages.",
    level3: "Given the simple_switch_13.py Ryu app, analyse its flow table performance: (a) how does it handle ARP vs IP packets differently, (b) what idle_timeout does it use for installed flows, (c) what happens to flow table size when 100 hosts ping each other in a full mesh. Modify the app to use a longer idle_timeout and measure the reduction in PACKET_IN rate.",
    level4: "Write a custom Ryu SDN controller application that: (a) discovers the network topology using LLDP packets (PacketIn handler), (b) computes the shortest path between any two hosts using Dijkstra on the discovered graph, (c) installs flow entries along the computed path for each new flow, (d) handles link failure: when a link goes down, recomputes paths for all affected flows and installs new flows, (e) exposes path information via a REST API at /topology/paths. Test with a 6-switch Mininet topology."
  },
  "u4t2": {
    level1: "Draw the OpenFlow pipeline showing: ingress port → Table 0 (flow table) → actions (output to port/set-field/goto-table/drop). Include: group table, meter table, and the controller channel. Label all Pipeline-302 fields. Write the purpose of each component.",
    level2: "Capture and decode 3 OpenFlow 1.3 messages using Wireshark: OFPT_FEATURES_REPLY (switch capabilities), OFPT_PACKET_IN (first packet of a new flow), OFPT_FLOW_MOD (flow entry installation by controller). For each: (a) draw the binary packet format with field sizes, (b) identify the transaction ID (xid), (c) explain the match-action semantics of the FLOW_MOD.",
    level3: "Modify the Ryu simple_switch_13.py to add VLAN-based isolation: (a) assign each switch port a VLAN ID based on configuration, (b) modify the flow installation logic to only allow communication between hosts in the same VLAN, (c) implement inter-VLAN routing via a 'router' host — install specific flows to redirect inter-VLAN traffic to the router. Test by pinging across VLANs (should fail) and through the router (should succeed).",
    level4: "Implement a custom OpenFlow 1.3 switch feature using Ryu and Mininet: (a) a packet-inspector application that counts packets per flow and reports top-10 flows (bytes/packets) via REST API every 10 seconds, (b) a flow-remover that removes flows from idle hosts (no traffic for 60s), (c) a rate-limiter that installs meter entries to limit specific flows to 1 Mbps, (d) a topology-viewer that draws the live network graph with link utilisation colours."
  },
  "u4t3": {
    level1: "Clone the Ryu controller and locate the `ryu/topology` module. Read the `switches.py` file to understand how Ryu discovers topology using LLDP. Draw a sequence diagram showing: Switch → PACKET_IN (encapsulated LLDP) → Controller → LLDP parsing → Port object creation → Link discovery.",
    level2: "Start a Ryu controller with `ryu-manager ryu/ryu/app/gui_topology/gui_topology.py` and a Mininet 4-switch linear topology. Open the web GUI (http://localhost:8080). Observe the discovered topology. Then manually fail a link using `sudo ip link set s1-eth1 down` and observe how long the topology view takes to update. Record the convergence time.",
    level3: "Write a Ryun REST API client in Python that: (a) connects to the Ryu REST API at http://localhost:8080, (b) GETs the current topology, (c) identifies all switches and links, (d) computes the shortest path matrix between all switch pairs, (e) compares the computed paths to the actual flows installed (query stats from switch), (f) reports any discrepancies (flows installed on non-shortest paths).",
    level4: "Build a multi-controller topology discovery service: (a) run 3 independent Mininet networks each with a separate Ryu controller, (b) implement a 'federation agent' Python script that polls each controller's REST API for its local topology, (c) normalises all links into a unified topology graph, (d) detects inter-domain links (links connecting switches in different domains) by matching overlapping IP subnets on external interfaces, (e) serves the unified topology via a single REST API, (f) draws the cross-domain topology using D3.js with domain colour coding."
  },
  "u4t4": {
    level1: "Read 3GPP TS 23.501 section on Network Slicing. Draw a diagram showing 3 slice types: eMBB (enhanced Mobile Broadband), URLLC (Ultra-Reliable Low-Latency), mMTC (massive Machine-Type). For each, list: latency target, bandwidth expectation, device density, and example use case (video streaming, autonomous driving, smart metering).",
    level2: "Using Docker, create 3 isolated network namespaces: (a) 'embb-slice' with Docker compose running a simple web server, (b) 'urllc-slice' with a UDP latency tester, (c) 'mmtc-slice' with 50 simulated IoT devices (Python scripts sending periodic data). Use Linux tc to apply different QoS policies to each namespace: eMBB gets 1 Gbps with 20ms delay, URLLC gets 100 Mbps with 1ms delay, mMTC gets 10 Mbps. Verify with iperf and ping.",
    level3: "Calculate the resource partitioning for 3 slices sharing a 10 Gbps link: eMBB requires 60%, URLLC 10%, mMTC 30%. Using weighted fair queuing, compute the minimum guaranteed rate for each slice. If eMBB is idle, how much bandwidth can URLLC and mMTC burst to? Implement this using Linux HTB (Hierarchical Token Bucket) and verify the burst behaviour with iperf.",
    level4: "Design and implement a network slice orchestrator: (a) Python CLI that accepts a slice definition YAML (SLA: bandwidth, latency, availability), (b) computes the required resources, (c) provisions isolated network resources using Linux network namespaces (one namespace per slice), (d) applies tc qdisc for bandwidth/latency guarantees, (e) assigns each slice a different VLAN tag, (f) monitors per-slice throughput and latency, (g) implements admission control — rejects new slice if remaining resources < 20%, (h) exposes a REST API for slice lifecycle (create/read/delete)."
  },
  "u4t5": {
    level1: "Define 3 observability pillars (Metrics, Logs, Traces) with: (a) a one-sentence definition, (b) a network monitoring example for each, (c) a tool that provides it. Create a comparison table with rows: Data type, Granularity, Retention, Query pattern, Example network use case.",
    level2: "Install Prometheus and node_exporter on a Linux VM. Configure node_exporter to expose system metrics. Write PromQL queries to: (a) compute average CPU utilisation over 5 min, (b) find the top 3 processes by memory usage, (c) calculate network bytes received per second, (d) alert when disk usage > 90%. Create a Grafana dashboard with 4 panels showing these metrics.",
    level3: "Deploy the TIG stack (Telegraf, InfluxDB, Grafana) using Docker Compose. Configure Telegraf to collect: (a) SNMP data from a simulated device (ifInOctets, ifOutOctets), (b) system metrics (CPU, memory, disk) from the host, (c) network statistics (tcp_established, udp_sockets). Create an InfluxQL query that joins interface metrics with system metrics by timestamp. Build a Grafana dashboard combining all 3 data sources.",
    level4: "Build an observability pipeline for a microservice network service: (a) deploy 3 containerised services (frontend, backend, database) with Docker Compose, (b) instrument each service with OpenTelemetry SDK to emit traces and metrics, (c) configure Prometheus to scrape metrics from each service, (d) configure Jaeger to receive traces, (e) create a Grafana dashboard showing service latency (p50/p95/p99), request rate, error rate, and a dependency graph, (f) introduce a latency injection (e.g., 500ms delay in database service) and observe the impact on the service dependency graph."
  },
  "u4t6": {
    level1: "Install and start a gRPC server example (using Python `grpcio`) that exposes a simple `GetMetrics` RPC. Write a gRPC client that calls `GetMetrics` every 5 seconds and prints the response. Explain the difference between gRPC and REST API for telemetry collection.",
    level2: "Set up a model-driven telemetry pipeline using Cisco IOS XE (or a simulator): (a) configure a device to stream CPU/memory/interface statistics via gRPC dial-out to a collector, (b) write a Python gRPC collector server that receives telemetry data, (c) decode the GPB-encoded telemetry and print the decoded values, (d) store the data in InfluxDB.",
    level3: "Deploy a Kafka-based streaming telemetry pipeline: (a) Telegraf → Kafka → Python consumer → InfluxDB → Grafana, (b) configure Telegraf to collect SNMP data at 10s intervals, (c) batch telemetry samples into Kafka messages every 30s (max 1000 samples per message), (d) implement backpressure handling: if Kafka consumer is slow, throttle producer by skipping every nth sample, (e) measure end-to-end latency from sample generation to dashboard update and identify the bottleneck.",
    level4: "Build a high-performance telemetry collector in Go (or Python with asyncio): (a) implements gRPC dial-in (device pushes) and dial-out (collector subscribes) modes, (b) handles YANG-push subscriptions with GPB and JSON encoding, (c) normalises multi-vendor telemetry (Cisco MDT, Juniper JTI, Huawei) into a common schema, (d) supports telemetry replay — store raw telemetry as Parquet and replay on-demand, (e) benchmark throughput (samples/sec) at 1000/5000/10000 samples/sec and report CPU/memory per throughput level."
  },
  "u4t7": {
    level1: "Install the ELK stack (Elasticsearch, Logstash, Kibana) using Docker Compose. Configure Logstash to ingest syslog from UDP 514. Generate 20 test syslog messages using `logger` and verify they appear in Kibana Discover. Create a simple dashboard showing syslog count over time.",
    level2: "Write a Python script that simulates a network traffic dataset with 1000 entries: timestamp, src_ip, dst_ip, bytes, protocol (TCP/UDP/ICMP). Load this into Elasticsearch. Use Kibana Lens to create: (a) a pie chart of protocol distribution, (b) a bar chart of top 10 source IPs by total bytes, (c) a line chart of traffic volume over time (1-minute buckets). Save as an exportable dashboard.",
    level3: "Use a provided 30-day traffic dataset (CSV) to: (a) train an Isolation Forest model (scikit-learn) to detect anomalous traffic patterns, (b) define 'normal' as 95% of historical traffic volume at the same hour-of-week, (c) flag any 10-minute window where traffic deviates > 3σ from the expected baseline, (d) cross-validate by checking if flagged windows correspond to known incidents in the dataset, (e) compute precision and recall of the anomaly detector.",
    level4: "Build an ML-powered network analytics pipeline: (a) Kafka → Python stream processor → ML inference → Elasticsearch → Grafana, (b) stream processor computes rolling window features (mean, std, entropy, trend) per metric per device, (c) pre-trained XGBoost model scores each window for anomaly probability, (d) anomalies (score > 0.9) are enriched with device metadata and indexed to Elasticsearch, (e) Grafana dashboard shows: anomaly timeline, top anomalous devices, feature contribution breakdown (SHAP values) for the active anomaly, and an 'auto-investigate' button that opens a correlate metrics view."
  },
  "u4t8": {
    level1: "Define 3 key SLA concepts: SLI (Service Level Indicator), SLO (Service Level Objective), SLA (Service Level Agreement). For each, write: definition, a network example (e.g., SLI = ping latency, SLO = <5ms for 99.9% of probes), and how breach is calculated.",
    level2: "Using Prometheus and Grafana, set up SLI monitoring for 3 metrics: (a) service availability — up/down probe every 15s from multiple locations, (b) latency — average response time for HTTP requests from 3 probe points, (c) throughput — bytes transferred per second. Compute SLO compliance: availability > 99.9%, latency p95 < 200ms, throughput > 1 Gbps. Display compliance as percentage for the last 24 hours.",
    level3: "Implement error budget burn-rate alerting: (a) define SLO: 99.9% availability over 30-day window (error budget = 43m 12s of downtime), (b) track error budget consumption in real-time using Prometheus, (c) compute burn rate over 1h and 6h windows, (d) alert if 1h burn rate > 2x (would exhaust budget in 15 days) for warning, > 10x (would exhaust in 3 days) for critical, (e) implement using Prometheus recording rules and Alertmanager.",
    level4: "Build a comprehensive SLA management platform: (a) define per-customer SLIs and SLOs in YAML config, (b) Python collector that measures SLIs from network probes (ping, HTTP, SNMP), (c) stores SLI data in TimescaleDB, (d) computes SLO compliance for every customer on 24h/7d/30d windows, (e) burn-rate alerting for each customer SLO, (f) predictive breach detection using linear regression on error budget consumption rate — forecast time-to-breach with ±1h confidence, (g) React dashboard: executive view (SLA health per customer), operational view (real-time SLIs, active breaches), predictive view (forecast charts)."
  },
  "u4t9": {
    level1: "Download the ITIL 4 Foundation document (free summary) and list the ITIL 4 Service Value Chain activities: Plan, Improve, Engage, Design & Transition, Obtain/Build, Deliver & Support. For each, write a one-sentence description of how it applies to NMS operations.",
    level2: "Create a mapping of ITSM processes to NMS functions: (a) Incident Management → Alarm Management & Ticketing, (b) Problem Management → Root Cause Analysis, (c) Change Management → Configuration Backup & Approval, (d) Service Level Management → SLA Monitoring, (e) Configuration Management → CMDB & Inventory. For each pair, write a 3-sentence explanation.",
    level3: "Design an ITSM integration workflow mockup (using draw.io or PlantUML): (a) Alarm raised in NMS → (b) Auto-create ticket in ServiceNow/Jira → (c) Categorise and assign based on alarm type → (d) Technician acknowledges and investigates → (e) Resolution steps logged → (f) Ticket closed → (g) RCA document created and linked. Show data flows between NMS and ITSM tool.",
    level4: "Build a ServiceNow (or mock) ITSM-NMS integration: (a) Python script that polls an alarm API (or mock server), (b) for each new Critical/Major alarm, creates a ServiceNow incident via REST API, (c) updates incident with alarm details (severity, source, timestamp, description), (d) polls incident status every 60s, (e) when incident is Resolved, auto-clears the alarm in NMS, (f) generates a weekly report: total tickets by severity, average time-to-resolve, most common alarm types, SLA breach rate, (g) email the report as PDF."
  },
  "u4t10": {
    level1: "Draw the ETSI NFV MANO architecture showing: NFVO (NFV Orchestrator), VNFM (VNF Manager), VIM (Virtual Infrastructure Manager), and the interfaces Or-Vnfm, Or-Vi, Vi-Vnfm, Os-Ma-nfvo. Label each component with its responsibility in one sentence.",
    level2: "Deploy a simple VNF using Docker: (a) create a Docker container running a lightweight router (e.g., Alpine with iptables), (b) connect it to 2 Docker networks (internal and external), (c) configure NAT so internal containers can reach the internet through this VNF, (d) verify connectivity. Document the steps and network topology.",
    level3: "Write a Python script that automates VNF lifecycle: (a) instantiate a containerised VNF from a Docker image, (b) configure initial networking (attach to pre-created Docker networks), (c) apply day-1 configuration via environment variables or volume-mounted config file, (d) monitor VNF health (container status, CPU, memory), (e) scale out: if CPU > 80% for 5 minutes, spin up a second VNF instance and update load balancer, (f) terminate: graceful shutdown and resource release.",
    level4: "Build a simplified NFV MANO orchestrator: (a) NFVO (Python/Flask) that accepts NSD (Network Service Descriptor) YAML defining VNFs and their connections, (b) VNFM (Python) that manages individual VNF instances via Docker SDK, (c) VIM adapter that interfaces with Docker or OpenStack, (d) instantiate a network service: 'vFirewall → vRouter → vDPI' chain, (e) implement auto-healing: if a VNF container crashes, VNFM detects (health check fails), restarts it, and signals NFVO, (f) provide a Grafana dashboard showing VNF status, chain connectivity, and auto-scaling/healing events."
  },
  "u4t11": {
    level1: "Write an intent statement in natural language for 3 network policies: (a) 'Sales team can access CRM server but not the finance database', (b) 'Video conferencing traffic should get priority over file downloads', (c) 'The network should survive a single core switch failure without dropping more than 5% of calls'. For each, identify the intent type (connectivity, QoS, availability).",
    level2: "Install ONOS SDN controller (Docker) and the intent-based forwarding application. Use the ONOS CLI to create an intent: `add-host-intent h1 h2` between two hosts in a Mininet topology. Verify that a flow is installed. Then use `intents` CLI command to list all intents. Remove the intent and observe the flows being removed.",
    level3: "Write a Python script that implements an intent compiler for simple connectivity intents: (a) parse a YAML intent file (`connectivity: {src: host1, dst: host2, bandwidth: 100mbps}`), (b) compute the optimal path using shortest-path first on the network graph (from Ryu REST API), (c) check bandwidth availability on all links along the path, (d) if path has sufficient bandwidth, install OpenFlow flows, (e) if not, reject the intent with a reason. Test with 5 intents on a 8-switch topology.",
    level4: "Design and implement a complete IBN (Intent-Based Networking) system: (a) intent language definition in YAML covering connectivity, isolation, QoS, and availability intents, (b) intent parser that validates syntax and semantics, (c) intent analyser using Z3 SMT solver — checks for conflicts (e.g., connectivity intent A→B conflicts with isolation intent A↛C if paths overlap), (d) intent compiler that translates verified intents to OpenFlow and NETCONF configurations, (e) intent monitor that verifies deployed intents are still satisfied (periodic state check), (f) intent lifecycle dashboard showing active intents, compliance status, and conflict history."
  },
  "u4t12": {
    level1: "Draw the 5G core SBA (Service-Based Architecture) showing the key NFs: AMF (Access & Mobility), SMF (Session Management), UPF (User Plane), PCF (Policy Control), UDM (Unified Data Management), AUSF (Authentication), NRF (NF Repository). Also show the interfaces N1-N4, N6, and the SBI bus. Label all components.",
    level2: "Deploy a free5GC or Open5GS core network using Docker Compose. Verify that the 5GC components (AMF, SMF, UPF, UDM, NRF) are running. Use the WebUI to register a subscriber. Use UERANSIM to connect a simulated UE and gNB. Verify the UE attaches to the network and gets an IP address. Capture the NAS signalling messages and identify the Registration Request/ Accept messages.",
    level3: "Configure network slicing in free5GC: (a) define 2 S-NSSAIs (Slice/Service Types): SST=1 (eMBB) and SST=2 (URLLC), (b) assign the UE to the eMBB slice by default, (c) configure dedicated UPF instance per slice, (d) use UERANSIM to connect the UE to the eMBB slice, (e) run iperf through the eMBB UPF, (f) capture GTP-U traffic and verify the TEID (Tunnel Endpoint ID) matches the configured slice. Measure eMBB UPF throughput.",
    level4: "Build a 5G network management dashboard: (a) extend the free5GC/Open5GS deployment with Prometheus exporters for each 5GC NF (AMF, SMF, UPF), (b) collect N4 session statistics (PDU sessions per SMF, active UPF tunnels), (c) collect AMF metrics (registered UEs, handover attempts), (d) collect UPF metrics (GTP-U throughput, packet drops), (e) create a Grafana dashboard with: per-slice UE count, per-slice throughput (line chart), NF health status (single stat), PDU session establishment success rate (gauge), top 5 UEs by data consumption (table), (f) configure an alert: if UPF throughput drops > 50% for 30s, page NOC via webhook."
  },
};
